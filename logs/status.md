# Project status

*Operational state + task queue. Updated at the end of every session. Strategic view: [wiki/project-roadmap.md](../wiki/project-roadmap.md).*

**As of:** 2026-07-12 · session 046 · **SLIDER RETURNS TO REST ON UN-PIN (Tom) — presentation/UI only; one line in `docs/app.js`.** On the v1.0.0 player, un-pinning a slider (pressing its AUTO chip, or Reset) cleared the label color, readout, and chip but **left the thumb where the user had dragged it**; because the range inputs paint with `accent-color`, a slider back on auto still showed the amber "filled" track and read as manually set (Tom's State 3 vs. the intended State 1). **Fixed in `syncCtl`:** the unpinned branch now resets a range input's value to `0` (its build-time rest value), so the thumb returns to the far left and the fill clears — matching the initial open state; the fix is centralized, so it also corrects the global **Reset** (both un-pin paths flow through `syncCtl`). Dropdowns keep their "choose…" placeholder path; only sliders needed the value reset. `SITE_VERSION` → **v1.0.1** (patch). The audio engine, determinism, and URL layout are untouched. **Verified:** a targeted headless drive of the exact move → pin → AUTO flow (State 3 byte-identical to the initial State 1; the Reset path too), a 2× before/after screenshot pair, and the standing gates — `site-smoke` **48/48**, `site-check` clean, **197** node tests. Details: `logs/sessions/046-2026-07-12.md`. **Prior state (session 045):** **UI REFINEMENT PASS ON THE v1.0.0 SITE (Tom's ten tweaks + a docs sync) — presentation/UI only; audio engine, determinism, and URL layout untouched.** Follow-up to the session-044 Console redesign (branch restarted from the merged `main`). Ten player tweaks: (1) dropdowns now show an italic **"choose…"** placeholder while on auto instead of a misleading concrete value (picking "choose…" again un-pins); (2) New piece + Continuous moved to sit beside Play; (3) the auto/pin + instruments explanations moved off the player into three new **About** sections (Using the controls / Choosing the instruments / The seed), the instruments text expanded to explain the ensemble is decided at composition time (uncheck reliably removes, check only sounds if the piece has a role for it); (4) **Send Feedback removed from the player** (code kept, `#fbDownload` listener guarded); (5) version → **v1.0.0** + a versioning convention added to `CLAUDE.md`; (6) a seed mouseover tooltip + longer About explanation; (7) **Reset relocated** to a right-aligned chip at the head of the AUTO column (same width/border as the AUTO chips, above Energy; `⟲` dropped); (8) `CLAUDE.md`/`continue-prompt.md`/`README.md` synced to the current state; (9) the now-playing status line no longer duplicates the section name (`playing m:ss · bar N`; section stays in the chips strip); (10) the 📱 emoji removed from the Background button. All in `docs/index.html` + `docs/css/site.css` + `docs/about.html` + the `docs/app.js` UI controller (`controlRow`/`syncCtl`/`refreshDynamicControls` placeholder logic, guarded feedback, `SITE_VERSION`, status string). **Verified:** `site-smoke` **48/48** (palette assertion updated to ignore the "choose…" placeholder), `site-check` clean, player (Start/Int/Adv) + About rendered headless at 2× and inspected, a playback capture confirmed the de-duplicated status line — 0 page/request errors. Details: `logs/sessions/045-2026-07-11.md`. **Prior state (session 044):** **CONSOLE SITE REDESIGN APPLIED (Tom's design handoff) — presentation only, engine untouched; fonts self-hosted; preliminary-tests links dropped from the public site.** Tom sent a from-scratch visual redesign ("Console": dark instrument-panel, hairline-ruled full-width sections, square corners, monospace-forward labels, one warm amber accent; Space Grotesk + JetBrains Mono) and asked to apply it per its README — **replace** `docs/index.html` + `docs/css/site.css`, **add** `docs/about.html`, **don't touch the engine**. Done as a byte-faithful drop-in after verifying the new markup preserves all 25 engine-referenced ids (24 static + engine-injected `inventBtn`) and every gate selector. **Two decisions from Tom during the session:** (1) the handoff loaded **Google Fonts from a CDN** — the project's first-ever runtime fetch, in conflict with `CLAUDE.md` rule 2 (no runtime network / must run from `file://`) and the must-stay-green smoke gate (offline `ERR_CONNECTION_RESET` ×15) — so, per Tom (AskUserQuestion), the two fonts are now **self-hosted** (both are variable fonts → one `latin` woff2 per family in `docs/fonts/`, `@font-face` in `site.css`, CDN `<link>`s removed from both pages); **no runtime fetch remains**. (2) Tom: "delete the Preliminary tests links — no longer necessary on the public site," so the `index.html` footer is back to the designer's **About · Source · CC0**, the clause was removed from `about.html`, and the obsolete `site-smoke.mjs` "footer links to preliminary tests" assertion was retired (the engine files stay as regression baselines; only the public links are gone). **Verified:** `site-smoke` **48/48** (one assertion retired), `site-check` clean, both pages rendered headless at 2× and eyeballed (Console look correct, self-hosted fonts loading, 0 page/request errors). Engine (`docs/app.js`, `docs/lib/**`, `docs/styles/**`) unchanged. Details: `logs/sessions/044-2026-07-11.md`. **Prior state (session 043):** **BACKGROUND MODE: PRE-RENDER THE NEXT TRACK (no gap between continuous tracks).** Tom reported a "quite long pause" between tracks in continuous background play (the render happening at the boundary; worse for longer pieces). Fixed by **double-buffering** (`docs/app.js`): while the current track plays, `prepareNext()` renders the next one (new seed, current style) to a ready WAV blob held in `prepared`; `advanceRendered()` swaps it in instantly (fallback to on-demand only if not ready). A `prepareToken` + session `bg.seq` discard superseded prepares (stop/style/seed change); `bgStop` drops the prepared blob; the continuous toggle starts/discards the prepare mid-playback. **Verified in-browser** (continuous short classical): next track pre-rendered ~10 s into the current one, hand-off measured **11 ms** (was a multi-second gap), track-after-next preparing again, 0 errors. Additive/background-only; live engine + URL layout + determinism untouched. Gates: 197 node tests, `site-check` clean, `site-smoke` 49/49. Details: `logs/sessions/043-2026-07-11.md`. **Prior state (session 042):** **BACKGROUND MODE WORKS ON-DEVICE ("works pretty well" — Tom); fixed abrupt track endings + answered platform-scope.** Tom tested the session-041 background PoC on his iPhone and reported one bug: in continuous background play, tracks ended on an abrupt cut instead of ringing out. **Fixed** (`renderOffline`, background-only): rendered files had no boundary shaping for non-fade endings, so a file could end mid-reverb (the live engine hides this by overlapping the next piece; a standalone file can't) — gave the reverb a fuller tail + a short safety fade to true silence over the final ~0.8 s; verified the last 50–200 ms of rendered cadence pieces is now digital silence. **Platform answer (Tom's question):** the *live*-engine background failure is worst on iOS but also affects Android (timer throttling); the *pre-render Background mode works on both iOS and Android* (not iOS-only) — natural exposure is mobile/touch devices, detectable via `pointer: coarse` + `maxTouchPoints`. **UI treatment (how prominent, device-gated?) is deferred to Tom's upcoming interface-design pass.** Gates: 197 node tests, `site-check` clean, `site-smoke` 49/49. Details: `logs/sessions/042-2026-07-11.md`. **Prior state (session 041):** **iOS BACKGROUND-AUDIO PROOF-OF-CONCEPT BUILT (Tom's request, behind a toggle).** After session 040 documented the limitation, this session built the pre-render path: a new first-party **`docs/lib/wav.js`** encodes the site's offline render to a 16-bit PCM WAV, and an experimental **📱 Background** toggle plays it through an off-screen `<audio>` element (unlocked in-gesture with a looping-silence clip so the async render can swap in) with **Media Session** lock-screen metadata/controls — the one path iOS keeps alive with the screen locked. Trades live-while-backgrounded (a settings/seed change re-renders) for durability; additive — the live conductor, URL layout, and determinism are untouched (`bgMode` isn't serialized). Gates: **197 node tests** (+5 WAV), `site-check` clean, `site-smoke` **49/49** (+step 10), and a one-off end-to-end browser check (real ~67 s piece renders → swaps in → plays → Media Session set → clean teardown, 0 errors). **Awaiting Tom's on-device test:** does it actually keep playing on a locked iPhone, and do the lock-screen controls work. Details: `logs/sessions/041-2026-07-11.md`. **Prior state (session 040):** **iOS BACKGROUND-AUDIO DOCUMENTED AS A KNOWN LIMITATION** — Tom asked whether the site's music can keep playing in Safari with the iPhone screen off; the honest answer is **not reliably** (iOS deliberately suspends Web Audio on screen-lock and grants background audio only to `<audio>`/`<video>` media elements — and a live generative engine is *doubly* blocked because the background JS timer that composes-ahead is also frozen, so keeping composition alive would need both a media-element bridge *and* worklet-based scheduling). **Decision (Tom, via AskUserQuestion): document, don't pursue** a fragile workaround — new section in `wiki/web-audio-fundamentals.md` ("Background and lock-screen playback"), a `findings-comprehensive-site.md` "Deliberately deferred" bullet, and a one-line honest note added to the site's About for iPhone listeners. No engine/site-behavior change; `wiki-lint` 0/100 · 192 node tests · `site-smoke` 45/45 all still green. Details: `logs/sessions/040-2026-07-11.md`. **Prior state (session 039) below:** **Phase 3d — TOM'S THIRD FEEDBACK PASS LANDED (4 requests): a real EXPRESSION LAYER in `perform.js` (phrase-analyzed legato overlap, portamento between slurred notes, deeper structure-aware rubato + agogics, per-phrase dynamics, arc-driven intra-note expression — the JIT loop STAYS: form is decided at `init()`, so expression knows each passage's role in the whole piece and stays live-tunable); THREE NEW RICH SUSTAINED VOICES (`organ`/`horn`/`voce`) wired into palettes/ensembles/Advanced checkboxes (V3 URL, 27-bit mask, old links decode); the INVENTED REPEATED-NOTE PATHOLOGY KILLED (Tom's linked piece = 61 identical notes running — root causes: session 038's gamut machinery read `theory.SCALES` offsets as step sizes so the "gamut" was part out-of-scale pitch classes; chorale top voice chord-locked over a drone; strata figuration parking — all fixed, with standing monotony gates in site-check + tests); JAZZ WITHDRAWN FROM THE UI (pack stays registered — URL enum frozen; reaction + revisit note in the wiki). Awaiting Tom's listen: does the classical lead now sing; do the new voices read as rich; invented variety.** (Session 038 built the dedicated invented composer; 037 widened its ensembles; 036/035 were feedback passes.) THE COMPREHENSIVE SITE IS LIVE at `docs/index.html` — the just-in-time composition loop, three-mode control surface, seven visible Start genres + Invent-a-style (Jazz hidden but registered), invented styles via the dedicated per-seed-kernel composer (`docs/lib/invent.js`), compact V3 URLs (V1/V2 still decode), continuous play, structure window. Gates as of 039: **192 node tests**, site-check sweeps clean across genres/meld/invent (now incl. a lead-monotony gate), bounded offline renders green (new-voice notes, two short invented pieces incl. Tom’s reported seed, one short classical), live smoke **45/45**, `wiki-lint` 0/100. **Notable finding (034):** byte-identical PCM is not browser-attainable (Chromium fan-in mixing rounds at ~1 ulp); determinism = identical events + audio within rounding accumulation (observed ≤ 3.5e-4, gate 2e-3) (`wiki/findings-comprehensive-site.md`). The five preliminary-test engines under `docs/preliminary-tests/` are untouched: 01 (tonal-classical **v0.3.1**), 02 (ambient-drift **v0.1.2**), 03 (groove-lofi **v0.2.1**), 04 (cantabile **v0.2.0**), 05 (percussion **v0.2.0**). **GitHub Pages is enabled — Tom listens via the public site (do not remind him).** Wiki: **100 pages** (72 reviewed + 28 drafts).

## Where things stand

- **Slider returns to rest on un-pin (session 046 — Tom; one-line UI fix on the v1.0.0 player).** Tom (three screenshots): when the app opens every control is on *auto* with the sliders inactive and the thumb on the far left (State 1); dragging a slider pins it and fills the track amber up to the thumb (State 2 — correct); but pressing that slider's AUTO chip left the thumb (and the amber fill) at the dragged position (State 3) instead of returning to State 1. **Root cause:** `syncCtl` (the single reconciler between a control's DOM and its pin state) handled the un-pin for the label color, readout, and AUTO chip (all driven by the `.pinned` class), and reset dropdowns to the "choose…" placeholder, but for a slider it only cleared the readout — it never reset the `<input type=range>` value. The range inputs are painted with `accent-color`, so a left-behind non-zero value keeps the amber fill; the control was logically on auto but still *read* as manually set. This was a conscious-but-wrong choice from session 045 (whose findings even noted "sliders were left as-is"). **Fix:** one added line in the unpinned branch of `syncCtl` — `else input.value = '0'` (the same rest value `controlRow` builds every range with, `min:'0' value:'0'`), so State 3 becomes byte-identical to State 1. Because `syncCtl` is shared, this also fixes the **Reset** button (`resetControls` → `syncAllControls` → `syncCtl`): Reset now returns all sliders to rest, not just clears their chrome. `SITE_VERSION` bumped **1.0.0 → 1.0.1** (patch). **Presentation-only** — the audio engine (`docs/lib/**`, `docs/styles/**`), determinism, and the compact-URL layout are untouched; `state.controls` already deleted the pin on un-pin, only the slider's visual lagged. **Verified:** a targeted headless Playwright drive of the exact flow (open → slider at 0, un-pinned, AUTO on = State 1 → drag to max = value 4, `.pinned`, AUTO off, readout shown = State 2 → click AUTO → **value 0, un-pinned, AUTO on, readout empty = State 3, a JSON-equal match to State 1**; the Reset-button path checked the same way) — 10/10 assertions, 0 errors; plus a 2× before/after screenshot pair (the "Space" slider, as in Tom's shots) confirming the amber fill disappears; and the standing gates — `site-check` clean, **197** node tests, `site-smoke` **48/48**. Wiki: **updated [findings-comprehensive-site](../wiki/findings-comprehensive-site.md)** (new "Slider auto-reset on un-pin — session 046" section: affordance symmetry + the `accent-color`-carries-state lesson; amended the 045 note). Details: `logs/sessions/046-2026-07-12.md`.

- **UI refinement pass on the v1.0.0 site (session 045 — Tom's ten tweaks + docs sync; presentation/UI only).** Follow-up to the Console redesign, branch restarted from the merged `main`. **Ten player tweaks:** (1) enum dropdowns show an italic **"choose…"** placeholder while on auto (via a `value=""` option + `.placeheld` class in `app.js` `controlRow`/`syncCtl`/`refreshDynamicControls`; picking "choose…" un-pins) instead of a misleading concrete value; (2) New piece + Continuous moved beside Play (dropped `.transportPrimary { margin-left:auto }`); (3) the auto/pin note + the instruments hint moved off the player into three new **About** sections (Using the controls / Choosing the instruments / The seed), the instruments text expanded to explain the ensemble is chosen at composition time (uncheck removes reliably; check only sounds if the piece has a role for that voice); (4) **Send Feedback removed from the player** — the `#fbNotes`/`#fbDownload` elements deleted, the `downloadFeedback` code kept and its listener guarded; (5) `SITE_VERSION` → **v1.0.0** + a versioning convention added to `CLAUDE.md` (patch/minor/major, bump on user-visible changes); (6) a seed `title=` tooltip + a longer About "The seed" section; (7) **Reset** moved out of the transport row to a right-aligned chip at the head of the AUTO column — same 3.6rem width + amber border as the AUTO chips, aligned above Energy, `⟲` dropped, and it spaces the style rail from Energy; (8) `CLAUDE.md`/`continue-prompt.md`/`README.md` synced (site built **and live at v1.0.0**, Console design, About page, `docs/css/` + `docs/fonts/` in the repo map); (9) the now-playing status line no longer duplicates the section name — it reads `playing m:ss · bar N`, section stays in the chips strip (`renderSections`); (10) the 📱 emoji removed from the Background button. **The audio engine, determinism, and URL layout are untouched** — all edits are markup/CSS + the `app.js` UI controller. **Verified:** `site-smoke` **48/48** (its palette assertion updated to ignore the "choose…" placeholder — checks the real sets still carry a "—" description), `site-check` clean, `wiki-lint` 0/100; player at Start/Intermediate/Advanced + About rendered headless at 2× and inspected; a playback capture confirmed the de-duplicated status line; 0 page/request errors on every page. Wiki: **updated [findings-comprehensive-site](../wiki/findings-comprehensive-site.md)** ("UI refinement pass — session 045"). Details: `logs/sessions/045-2026-07-11.md`.

- **Console site redesign applied (session 044 — Tom's design handoff; presentation only).** Tom sent a from-scratch visual redesign and asked to apply it per its README (replace `docs/index.html` + `docs/css/site.css`, add `docs/about.html`, don't modify the engine). The new look — **"Console"**: near-black, hairline-ruled full-width sections, **square corners**, monospace-forward labels, one warm amber accent (`#f0a848`), Space Grotesk (display/prose) + JetBrains Mono (all technical labels), progressive disclosure via the existing tier toggle, plus a standalone **About** page. Applied as a byte-faithful drop-in after verifying the new markup keeps every engine-referenced id (24 static + engine-injected `inventBtn`, `mode0/1/2`) and every gate selector (`#structDesc` is kept in the DOM, hidden by CSS). **Font-delivery decision (Tom):** the handoff's Google Fonts CDN `<link>` was the project's first runtime external fetch (conflict with `CLAUDE.md` rule 2 + the must-stay-green gate), so the two fonts are now **self-hosted** — both are variable fonts, so one `latin`-subset woff2 per family lives in `docs/fonts/` (`space-grotesk-latin.woff2` 22 KB, `jetbrains-mono-latin.woff2` 31 KB), declared via `@font-face` in `site.css` (`font-weight: 400 700`, `swap`), CDN `<link>`s removed from both pages; **no runtime fetch remains, site runs from `file://`**. **Public-site cleanup (Tom):** "delete the Preliminary tests links — no longer necessary on the public site" → `index.html` footer reverted to the handoff's **About · Source · CC0**, the clause removed from `about.html` (design-docs link + authorship + CC0 kept), and the obsolete `site-smoke.mjs` "footer links to preliminary tests" assertion retired (the preliminary-test engine files stay untouched as regression baselines; only the public links are gone). **The engine is genuinely untouched** — presentation-only. Verified: `site-smoke` **48/48** (one assertion retired), `site-check` clean, both pages rendered headless at 2× and eyeballed (Console look correct, self-hosted fonts loading, 0 page/request errors). Wiki: **updated [findings-comprehensive-site](../wiki/findings-comprehensive-site.md)** ("Console redesign — session 044"). Details: `logs/sessions/044-2026-07-11.md`.

- **Background mode: pre-render the next continuous track — no gap (session 043).** Tom (on-device): "there was quite a long pause when the song length was set to medium… probably even longer with long." The pause was the next track rendering *at* the boundary. **Fixed by double-buffering** (`docs/app.js`, background-only): while the current track plays, `prepareNext()` renders the next piece (a fresh seed, current style) to a ready WAV blob held in `prepared`; on track end, `advanceRendered()` **swaps it in instantly** rather than rendering on demand (it falls back to on-demand only if the render isn't ready — e.g. the first transition, or a render slower than the track). Correctness/lifecycle: a `prepareToken` plus the session `bg.seq` make any superseded prepare (a stop, or a live style/seed change → `playRendered`) discard itself and revoke its blob; `bgStop` drops `prepared`; toggling Continuous during background playback starts or discards the prepare; the render is off-main-thread so the playing `<audio>` is unaffected. **Verified in-browser** (continuous, short classical, real full renders): the next track was pre-rendered ~10 s into the current one, and the boundary hand-off measured **11 ms** (vs the multi-second render gap before), with the track-after-next then preparing again — 0 console errors. Memory: two in-flight WAV blobs instead of one. A residual sub-second `<audio>` decode at the swap is possible (a second `<audio>` element for true zero-latency was rejected — a fresh element lacks the gesture activation the reused one has, an iOS risk). Additive; the live engine, URL layout, and determinism are untouched. Gates: 197 node tests, `site-check` clean, `site-smoke` 49/49. Details: `logs/sessions/043-2026-07-11.md`.

- **Background mode: on-device success + graceful-ending fix (session 042).** Tom tested the session-041 PoC on his iPhone: **"works pretty well."** One bug reported — in continuous background play the first few tracks ended on an **abrupt cut** instead of fading out on the last note. **Root cause:** a rendered file's length is `musicSec + tail`, and for non-fade endings (cadence/stop/ringout) nothing shaped the file boundary, so it could end while the reverb was still ringing; the live engine masks this by overlapping the next piece in continuous play (`advancePiece`), but a standalone rendered file has nothing after it. **Fix** (`docs/app.js` `renderOffline` only — live playback and the render gate use separate code paths): fuller reverb tail (`0.6 + reverbSeconds·1.1`, was `0.4 + ·0.8`) + a short safety fade to true silence over the final ~0.8 s for non-fade endings. **Verified** in-browser: rendered cadence pieces now have digital silence (peak 0) in their last 50–200 ms across 3 seeds, music level untouched (peak ~0.5–0.7). **Platform-scope answer (Tom's Q):** the live-engine-stops-in-background problem is worst on iOS (hard `AudioContext` suspend) but also affects Android (background timer throttling starves the compose-ahead scheduler); the **pre-render Background mode works on both iOS and Android** (Android Chrome is more permissive; the Media Session wiring already gives Android a media notification — Android battery-optimization settings can still interfere). So it's **not iOS-only**; natural exposure is mobile/touch devices, detectable via `matchMedia('(pointer: coarse)')` + `navigator.maxTouchPoints` (robust vs UA sniffing). **The actual UI treatment is deferred to Tom's upcoming interface-design pass.** Gates: 197 node tests, `site-check` clean, `site-smoke` 49/49. Details: `logs/sessions/042-2026-07-11.md`.

- **iOS background/lock-screen audio — proof-of-concept built (session 041, Tom's request).** After documenting the limitation (session 040), built the pre-render path behind an experimental **📱 Background** toggle. **New first-party lib `docs/lib/wav.js`** (UMD-lite, CC0): `encodeWav(audioBufferLike)` → 16-bit PCM RIFF/WAVE from the site's existing `renderOffline` stereo buffer. WAV because it's the only container producible in pure vanilla JS (no dependency, no build step) and plays on every iOS version; cost is size (~172 KB/s → ~11 MB for a 67 s piece, fine for transient in-memory use). **The flow (`app.js` `playRendered`):** render → `encodeWav` → `Blob` → `URL.createObjectURL` → off-screen `<audio>`; the iOS gesture requirement (media must start in a tap, but the render is async) is handled by playing a **looping-silence WAV in-gesture** to claim the media session, then swapping the real file's object URL in when ready. **Media Session** gives the lock-screen card (title/artist/generated artwork) + play/pause/next/prev handlers (next reseeds+re-renders; continuous re-renders on `ended`). `bgStop()` (folded into `stopAll`) pauses, clears src, revokes the URL. **Trade-off:** a rendered piece is fixed, so live no-restart changes don't apply while it plays — a style/seed change re-renders (brief "composing…"); it's a mode, not a replacement. Nothing serialized (`bgMode` is a session toggle, like `continuous`), so links/URL layout unchanged. **Validation:** 197 node tests (+5 WAV: header/interleave/clamp/mono/silence), `site-check` clean, `site-smoke` 49/49 (step 10 checks the toggle + `<audio>` + render→WAV→blob RIFF/WAVE header), one-off end-to-end browser check green (real ~67 s classical piece renders, swaps in, plays with playhead advancing, Media Session "Classical"/"playing", clean teardown, 0 errors). **The one thing headless can't test is the actual locked-iPhone behavior — that's Tom's on-device check.** If it disappoints, fallbacks are in `wiki/web-audio-fundamentals.md`. Details: `logs/sessions/041-2026-07-11.md`.

- **iOS background/lock-screen audio — documented, not pursued (session 040).** Tom's question: can the music keep playing in Safari when the iPhone screen switches off? **Answer: not reliably** — it's an iOS platform restriction, not an app bug. iOS suspends the `AudioContext` on screen-lock/backgrounding (WebKit Bug 237878) and reserves background audio for `<audio>`/`<video>` media elements; the site is pure Web Audio (`docs/app.js` → `ctx.destination`, no media element) driven by a `setTimeout` compose-ahead scheduler, so a *generative* engine is blocked **twice** (suspended context **and** frozen background timer). The only *reliable* background route is pre-rendering a piece to a file and playing it through an `<audio>` element — which forfeits the live composition that is the site's whole point; the MediaStream→`<audio>` bridge is flaky on iOS and still needs worklet scheduling; Screen Wake Lock (iOS 16.4+) only keeps the screen *on*. **Tom chose to document it.** New `wiki/web-audio-fundamentals.md` §"Background and lock-screen playback" holds the mechanism + option tradeoffs + the resume-on-`visibilitychange` minimum; `findings-comprehensive-site.md` "Deliberately deferred" carries the product-constraint bullet; the site's About gained one honest sentence. **If revisited:** the cheap, safe first step is a resume-on-`visibilitychange` handler (recovers cleanly on return, worth doing on its own); the full background attempt is a larger on-device-verified effort — scope before starting. No engine/site-behavior change. Gates: `wiki-lint` 0/100, 192 node tests, `site-smoke` 45/45. Details: `logs/sessions/040-2026-07-11.md`.

- **Tom's third feedback pass (session 039 — 4 requests: voices · expressivity · invented variety/repeated notes · remove Jazz).** (1) **Expression layer** (request 2): Tom asked for legato phrasing, more rubato, and expression-as-a-separate-step informed by each passage's role in the whole piece — possibly at the cost of the compose-on-the-fly model. **Decision: the JIT loop stays** — the piece's form (sections/kinds/intensities/arc/total bars) is fully decided in `strategy.init()` before any note sounds, so `perform.realize()`'s new expression pass reads structure (units now stamped with `tPiece`/`arcLevel` by the composer wrapper; notes carry weight/apex/cadence/phraseStart tags) and shapes: phrase-analyzed **legato overlap** (slurred different-pitch notes overlap ≤ ~140 ms; repeats re-articulate; phrase ends breathe; style-gated — classical binds at ~0.84, lofi stays détaché at ~0.3), **portamento** between slurred notes on sustained voices, **deeper rubato** (≤ ~±9% phrase-arch for lyrical styles, quiet passages breathe more than peaks) + agogics (phrase-entry hesitation, apex lean), **per-phrase dynamics** (crescendo into each phrase's apex), and arc-driven intra-note expression. Expression stays live-tunable. Also fixed three latent 034 gaps: `scoop` was never consumed (silent since build), `swellPos`→`swellPeak`, `drift` never set (microDrift was off site-wide). (2) **Richer voices** (request 1): three new original sustained expr-reading voices in `synth.js` — **`organ`** (beating detuned ranks + delayed tremulant), **`horn`** (lowpass opens through the note — the brass bloom), **`voce`** (morphing vowel formants + breath + deep vibrato) — level-matched, wired into 2 new generic palettes (every style), 1 new authored palette each for classical/ambient/cinematic/folk, 8 new invented ensemble templates (→40), and the Advanced checkboxes (`MASTER_INSTRUMENTS` 24→27 → **V3 URL layout**, append-only; V1/V2 links decode; snapshot tests + `_selftest` updated). (3) **Invented repeated-note pathology KILLED** (request 3; Tom's link = 61 identical lead notes running): root causes were **session 038's gamut machinery reading `theory.SCALES` offsets as step sizes** (the "identity gamut" was partly out-of-scale pitch classes; when its accidental overlap with the true scale spanned 1–2 pitch classes the melodic pool collapsed — also mis-pitched the tintinnabuli T-voice), the **chorale top voice hard-locked to chord tones over a drone** (fixed: chorale+drone→modal coupling + a contour/repeat-penalty chooser), and **strata figuration parking** on held skeleton tones (fixed: neighbor-swing). Standing guards: `site-check` fails any non-percussion piece whose melodic lead runs one pitch > 16× or uses < 4 distinct pitches; node tests pin Tom's exact seed (`c069e05b`: now 12 distinct, max run 2, voce lead) + a 24-seed sweep + gamut-⊆-scale. Variety: drone weight trimmed 0.18→0.14; chorale/tintinnabuli usually remap struck leads to sustained voices. Invented seeds redraw (third session running). (4) **Jazz withdrawn from the UI** (request 4): `HIDDEN_STYLES` in `app.js` (Tom: "least pleasing… does not sound at all like jazz… not ready to be shared"); the pack stays registered (pack order = URL genre enum; old jazz links/melds still play); reaction + what-it-would-take-to-return note in `findings-comprehensive-site.md`. **Validation:** 192 node tests (+12, 2 updated), all site-check sweeps clean, isolated-note renders of the new voices clean (no clicks, true-zero tails, ~−30 dBFS), `render-invent-quick` green on Tom's seed + a horn/percussion case, a short classical offline render green, `site-smoke` **45/45**, `wiki-lint` 0/100. **Awaiting Tom's ear:** does the classical lead sing; rubato depth tasteful; do organ/horn/voce read as rich (voce pleasing or uncanny); invented variety + any residual repeated-note pieces. Details: `logs/sessions/039-2026-07-11.md`.

- **The dedicated invented-style composer (session 038 — Tom's request 3, the deferred big one).** Invented styles stopped routing to the eight genre strategies. **Design (from an original reading of the wiki — full rationale in `wiki/findings-invented-composer.md` + the session log):** a style is a small *generative system*, so `invent()` now draws a per-seed **style kernel** — (1) one of **8 texture architectures** (melody+accompaniment, ostinato web, call-and-response, gamelan-like strata, canon, kotekan-style hocket, chorale homorhythm, Pärt-style tintinnabuli), (2) a **rhythm system** (flow · one Schillinger resultant cell stamped everywhere · a clave/bell/Euclid/sieve timeline · a kit groove), (3) a **melodic gamut** (seeded 5–7-degree subset of the scale — an invented mode-within-the-mode; harmony keeps the full scale) + **2 hierarchy pillars** (frequent/long/goal degrees — hierarchy taught by distribution), (4) per-texture params (canon delay/interval, strata density multiplier, tintinnabuli position/side, ostinato cycle lengths), with **coherence couplings** (tintinnabuli→drone/modal; chorale→density+tempo caps; hocket→motion floors; a general chord-rate ceiling) — and the new **`docs/lib/invent.js`** (one strategy, registered via a new `compose._setInvented()` hook, deliberately NOT a registry pack so the URL genre enum is untouched) realizes any kernel: a functional **form grammar** (state/return/depart/break/peak/end; at least one recognizable return always; late peak; long pieces extend as rondos instead of inflating sections; composed ending idioms incl. the cadenceDrop signature as the style's own cadence formula), a committed **theme** (motif+contour invariant across returns), and **full-ensemble voicing** — the percussion writer runs for every kernel (inviolable timeline, kick with beat 1 sacred, backbeat+ghosts, velocity-hierarchy hats, two hand-perc parts on complementary Euclidean rotations, colotomic boom, section gongs; arc-gated layering; per-texture quietness scales velocity, never existence). The structure window names the drawn architecture for Tom. **Validation:** 182 node tests (+5 new — kernel validity, architectural variety ≥4 textures/40 seeds, every-drawn-role-voiced ×12 seeds, determinism+real ending, recognizable theme return — and 1 updated: the invented strategy is registered, not a registry pack), site-check invent sweep 12/12 + genre/meld regression sweeps clean, a bounded offline-render gate on two short invented pieces (new `experiments/tools/render-invent-quick.mjs`; the full render sweep stays impractical in this slow environment — the 036–037 queued full sweep stands), live smoke re-run, wiki-lint 0/100. **Old invented links now play different (new-engine) pieces from the same seed; presets/melds unaffected.** **Awaiting Tom's ear:** do the 8 architectures read as different engines; is the gamut audible as identity; texture weights; percussion quietness in quiet textures. Details: `logs/sessions/038-2026-07-10.md`.

- **Invented-style refinements (session 037, `docs/lib/style.js` `invent()` only).** (1) The invented-style **ensemble templates grew 6→32** — each register-balanced (a high `lead`, a mid `comp`/`pad`, a low `bass`/`drone`) with **~half adding percussion** (kit or hand/world), reusing the shared `ROLE_REG`/`ROLE_LVL` tables — so different invented seeds give far more varied instrumentation. (2) Invented **names** gained a lowercase letter + 3-digit number ("Invented Nocturne c402"): ~208k labels vs 792 (~260× fewer collisions), still cosmetic/seed-derived. **Reaffirmed limitation:** percussion (and roles beyond the routed strategy's vocabulary) only *sound* on groove routings — full-ensemble voicing awaits the dedicated invented composer. **Validation:** 177 node tests (+4: ensemble variety, register balance, percussion fraction ~half, name format), Node sample, and a browser check that an invented style plays end-to-end (0 errors). This changes what invented *seeds* produce; presets/melds unaffected. **Handed to Fable next:** request 3 — a **dedicated invented-style composer** (a newly-generated engine per invented piece: original combinations of high-order musical characteristics that a human could find appealing, not routed to the eight genre engines); deferred here because it needs an original reading of the wiki. Details: `logs/sessions/037-2026-07-10.md`.

- **Tom's second feedback pass landed (session 036, Phase 3d — nine requests, `docs/` only; audio engine `compose`/`perform`/`synth`/`fx` untouched).** (1) **Intermediate palette dropdown** now shows **8 sets with descriptions** (3 authored, auto-sampled + 5 shared generic recolor sets, opt-in). (2) **Advanced replaces the palette dropdown with a 24-instrument checkbox array** — check to add, uncheck to remove; backed by a bitmask over a frozen master voice list, rebuilding the ensemble. *Removal reliably mutes; adding sounds only if it fills a role the strategy voices (role-based composition limit).* (3) **Five new Advanced params** mapping onto fields the engine already read: micro-timing (`laidBack`), rubato, chord rate (`harmonicRhythm`), melodic motion (`stepBias`), melodic range. (4) A **Reset button** (right of Continuous) returns all pins to auto. (5) Continuous play now leaves a **~1 s gap** between pieces. (6) The **playhead** is centered, wider, high-contrast. (7) **Min piece length ~60 s** (short target 65→60; per-genre auto minimums lowered) for quick auditioning. (8) A **structure window** below the viz ("what this piece is doing", generalized from Engine 04: key/meter/tempo/harmony/arc/ending/instruments + a live section strip + invented-style signatures). (9) **Invent a style** added to Start. **Infrastructure:** the compact URL gained a **V2 append-only layout** (`serialize.js`) — six new controls + `palette` widened 2→4 bits; `encode` writes V2, `decode` still reads old **V1 links** (new controls default to auto); empty payload still 14 chars, full-pin 36. **Validation:** 173 node tests (+6), `site-check` OK, `site-smoke` step 9 added, offline render gate re-run, a Node V2 round-trip check (incl. back-compat), and a 15/15 one-off headless verify across all nine requests (0 console errors). Durable decisions: `wiki/findings-comprehensive-site.md` ("Second feedback pass (session 036)"). Details: `logs/sessions/036-2026-07-10.md`.

- **Tom's first-listen UI feedback landed (session 035, Phase 3d — first tuning pass, `docs/` only).** Tom tried the live v0.1.0 build and opened a multi-day feedback series; this session applied his first set — all UI/site changes, **no engine/library or genre-pack code touched** (so the 167 node tests, `site-check`, and the offline render gates are unaffected). **Changes:** (1) **Style selection is single-select** — the Start/Intermediate/Advanced picker now chooses exactly one style at a time (radio semantics: a new pick clears the old one; re-clicking the sole selection is a no-op). The two-genre **meld stays in the engine** (`style.buildVector`/`serialize` still accept two parents; a legacy meld URL still plays; `site.test.js` meld assertions unchanged) but the button UI no longer *creates* one. (2) **The Play box moved to the top**, directly under the mode toggles (Style + controls follow it). (3) **Continuous play** — a new ↻ Continuous toggle: at each piece's music-end the site auto-advances to a fresh piece in the current style with a **new random seed**, gaplessly (the outgoing piece's reverb/release tail overlaps the new attack; no crossfade, no silence). Mechanism: `advancePiece()` + a new conductor `musicEndAt` getter, gated in `tickUi`. (4) **"copy" → "copy seed"** and the "composed while it plays…" **intro paragraph moved into About** (blending no longer described as a user action). **Validation:** `site-smoke.mjs` **36/36** (added step 8 for continuous play; the single-select swap now reads "Classical → Ambient"), 167 node tests, `site-check` OK, offline render gates green, plus a one-off end-to-end check that a short piece auto-advances at its ~64 s end (no manual hook), still playing, 0 errors. Durable decisions filed in `wiki/findings-comprehensive-site.md` ("Product decisions from Tom's first listen"). Details: `logs/sessions/035-2026-07-10.md`.

- **THE COMPREHENSIVE SITE IS BUILT (session 034, Phase 3c — one session, walking-skeleton order, all of M1–M8).** `docs/index.html` is now the site itself (the early landing page it replaces is folded into its About/footer; the preliminary-tests and design-docs links stay). **What works:** all **eight Start genres** compose and play end-to-end (classical & ambient written by the orchestrating session as the skeleton + free-time proof; lofi/jazz/folk/electronic/cinematic/percussion by six parallel subagents — Opus 4.8 for the new builds jazz/folk/electronic + the percussion port, Sonnet 5 for lofi/cinematic — against a written strategy contract, every pack 12/12-clean on the symbolic sweeps); **two-genre melds** per the schema (chassis strategy + partner harmony, compatibility gates, even balance; five pairs swept clean); **live setting changes without restart** via the just-in-time conductor (a control change swaps the vector; the next composed phrase reads it; instant surface params ramp in place; re-plan controls rebuild the remaining plan; a genre/blend/invent swap runs the **two-bar equal-power crossfade** between two whole master chains — smoke-tested headless: playhead monotonic across changes, no stop); **invented styles** at Int/Adv (novelty budget 1–3 axes, 2–3 signature rules realized centrally — motto→motif, interval-cell melodic bias, voicing habits, echo tail — with strategy routing + hostability repairs); the **compact URL** (v1 frozen layout: 13 chars nothing-pinned / 28 all-pinned; seed editable/copyable; unknown versions → defaults); the three-mode UI with auto/pin chips on every control; piano-roll viz; feedback JSON. **New first-party libraries** (all original, annotated, CC0): `docs/lib/generators.js` (Schillinger resultants, Xenakis sieves, Koenig tendency masks, the 9 arcs), `style.js` (the vector, regions, meld, invention, coherence, the control registry), `compose.js` (harmony/melody/voicing/rhythm machinery + the per-unit-stream incremental composer), `perform.js` (rubato/swing/accent/expression performer), `serialize.js` (bit-packed base64url), plus the six proven libs vendored (`fx.js` gains site-local `out` routing + mid/side `setWidth`). **Validation:** 167 node tests (`site.test.js` new); `site-check.js` (new, fast symbolic gate) 12/12 sweeps across genres/melds/invent; `render-site.mjs` (new, per-genre OfflineAudioContext gates) green — peaks ≤ ~0.71, no gaps, −16..−18 dBFS; `site-smoke.mjs` (new, live headless) 16/16; engines' own gates untouched and green. **Finding filed** (`wiki/findings-comprehensive-site.md`): byte-identical PCM is unattainable in Chromium (fan-in summation order → ~1-ulp intermittent diffs); the site’s determinism contract is identical events + audio within rounding accumulation — plus the JIT/unit-grain lessons (notes outlive units; chord-slot tiling; role-name fallbacks for melds). **Awaiting Tom:** the first listen across genres/melds/live-changes; preset tuning and meld balance are deliberately first-pass. Details: `logs/sessions/034-2026-07-09.md`.

- **Third/final Phase-3b design doc — the site & library architecture (session 033); Phase 3b complete.** Wrote `docs/design/site-architecture.html`: the **folder layout** (the site is `docs/index.html` + `docs/lib/` + `docs/styles/` + `app.js`; classic scripts into one global namespace, `file://`-runnable per `findings-shared-lib-foundation.md`; `preliminary-tests/` and `design/` are separate auxiliary folders); the **module list** (existing `rng/transport/theory/synth/fx/analysis` + new `generators`/`style`/`compose`/`perform`/`serialize`, with the incremental `compose` unifying the five engines' composition families as the hard central task); the **runtime pipeline** and the **just-in-time composition loop** (a conductor composes a bar/phrase ahead of the playhead reading the *live* style vector — the mechanism that makes live changes work with no restart); **live changes** mapped to the three response speeds + the **two-bar crossfade** for a genre/blend swap (fade between two `fx` sub-buses, both running their JIT loop over the overlap); determinism/seeding (named RNG streams; the vector gates every draw; offline==online); the compact URL; how the 5 engines' voices/composers/performers/UI **generalize**; and a **walking-skeleton-first build order** — M1 skeleton → M2 one genre end-to-end → M3 three-mode UI + compact URL → M4 all 8 genres → M5 melds → M6 live changes → M7 invented styles → M8 polish — with **M1–M4 as the realistic one-session floor** and every milestone additive. Per-genre offline-render + ui-smoke gates specified. All four design pages render clean headless (0 console errors). **Then, per Tom's request, wrote a self-contained build prompt for Fable** to do the build (orchestrated, walking-skeleton-first) in one session — preserved verbatim in the session log and handed to Tom. Details: `logs/sessions/033-2026-07-09.md`.

- **Second Phase-3b design doc — the style-vector schema (session 032).** Tom answered the three follow-ups the control-taxonomy review raised — (1) two-genre **meld balance = even (50/50)** for now (flagged provisional: "I have no idea how this genre meld will actually sound… adjustments will probably be made"); (2) a live genre/blend swap = a **two-bar crossfade** (also provisional); (3) the shared link stores **only the destination snapshot** — and said proceed. Wrote **`docs/design/style-vector-schema.html`** (companion to the control taxonomy): the **style vector** as a fielded structure (~24 fields in 4 layers — material / per-voice / evolution / surface — each typed categorical/ordinal/continuous, which drives both blending and serialization); how **presets** (hand-tuned points), **invented styles** (seeded samples + novelty budget + 2–3 authored signature rules, EMI-minus-corpus), and **melds** instantiate it; the **meld** in full (§5: per-axis parent selection, *not* averaging — categorical fields snap to one parent, continuous blend; rhythm-chassis and harmony/mode from *different* parents; even balance; compatibility gates for tuning/meter/feel/rubato; cap 2 parents; worked examples); the **coherence** gate; the **determinism + live-morph** data flow (§7, incl. the 2-bar crossfade); a **serialization** sketch (§8, destination-snapshot only; pinned settings + seed as versioned bit-packed base64url per music-representation); and the **three shared generators** (Schillinger interference rhythms, Xenakis sieves, Koenig tendency masks). Built on `style-invention-and-style-space.md` + `meta-composition-and-style-machines.md` + `hybridization-and-fusion.md` + `music-representation-and-notation.md`. **Reconciled the control taxonomy** to match (even balance not seed-varied; 2-bar crossfade; destination-snapshot-only; linked the new doc). All three design pages render clean headless (0 console errors; taxonomy still 8 genre rows). **Awaiting Tom's review**; the natural next doc is the **site & library architecture** (P3-2c). Details: `logs/sessions/032-2026-07-09.md`.

- **Tom's control-taxonomy decisions folded in + live-change design added (session 031).** Tom reviewed the session-030 taxonomy and decided: keep the 8 Start genres but **broaden Folk** from Celtic-only to pan-European (melding Celtic/Nordic/Baltic/Balkan/Iberian/etc., not reproducing one); keep the 5 Start dials but make **Genre multi-select, max 2, where two selections blend** into a melded style (a two-*preset* meld following `hybridization-and-fusion.md`'s per-axis-parent logic — not on-the-fly invention, which stays Intermediate/Advanced); **Energy** adapts per genre and per combination; the Intermediate + invented-parameter sets **accepted** (to refine after he experiments); **three distinct modes with a top selector**, settings preserved. Revised `docs/design/control-taxonomy.html` to **rev. 2** accordingly. **New requirement — live setting changes:** Tom asked whether the piece can change at the current playing point without restarting (the test engines restart, hiding the difference) → **confirmed feasible**, and added §8 designing it: the site composes **just-in-time** (a bar/phrase ahead of the playhead, reading current control values) instead of the test engines' compose-whole-piece-up-front; three response speeds (**instant** mix/sound · **next bar/phrase** local-structural · **re-plan-from-here** for form/genre changes, with a transition); the **URL stays a compact current-settings snapshot** (sharing reproduces the destination played from the top, not the exact live morph — an optional automation-capture is deferred). This is a first-class architectural commitment for the site engine (folds into the site-architecture doc + `engine-architecture.md`). Both design pages render clean headless (0 console errors, 8 genre rows). **Also this session — Folk backing:** added a **Baltic** section to `european-folk-and-dance.md` (Lithuanian sutartinės canon-in-seconds + kanklės/kokle/kannel drone box-zithers + skudučiai hocket pan-pipes + Estonian regilaul/Seto/Suiti/torupill drone song), via a research subagent — cited sources, honest hedges on the unverified claims, plus 2 engine-implications bullets; lede/summary/related-pages updated (five → six clusters); `wiki-lint` **0/98**. Enters `draft`, awaiting the standard verification pass. Now the broadened Folk Start genre is fully backed. Details: `logs/sessions/031-2026-07-09.md`.

- **First Phase-3b design doc delivered — the control taxonomy (session 030).** Per Tom's request, wrote `docs/design/control-taxonomy.html` (+ a `docs/design/index.html` review-docs index): the concrete controls at each tier — **Start** (~5 headline dials + Genre + Play/dice), **Intermediate** (~20 familiar controls, grouped, each with type/range/default/mapping), **Advanced** (the 13 style-vector axes + per-layer overrides + **8 invented parameters**: intensity arc, development mode, timeline type, interlock density, novelty budget, style blend, coherence strictness, signature emphasis) — plus seven design principles (auto=seed-decides · deterministic inputs · progressive disclosure · perceptual labels/scaling · defaults-are-the-product · constrained randomize · failure-mode guards) and a **proposed 8-genre Start shortlist** (Classical · Ambient · Lo-fi beats · Jazz · Folk/Celtic · Electronic/House · Cinematic · Percussion). The shortlist was grounded by a research subagent against the wiki genre pages (tempos/character cited; 5 map to existing engines, 3 are new builds; Blues and Minimalism flagged as swap-ins). Built on `control-surfaces-and-user-parameters.md` + `style-invention-and-style-space.md`; rendered clean headless (0 console errors, 8 genre rows present). **Awaiting Tom's review** — seven explicit questions in §9, the Start shortlist (Q1) first; once he signs off the settled taxonomy graduates from the review doc into the wiki. Details: `logs/sessions/030-2026-07-09.md`.

- **Tom's 2026-07-09 answers propagated + `docs/` reorganized (session 029).** Tom answered the four comprehensive-site questions (now in `project-open-questions.md` → "Answered"): (1) **Engines 01–05 stay live but move to a new `docs/preliminary-tests/` subfolder**; (2) the new site's **`index.html` goes directly under `docs/`** as the public entry point he'll share once it matures; (3) **~6–10 Start genres with Claude proposing the shortlist** — confirmed; (4) the design-review cadence stands, and **documents for his review now go in `docs/`** (temporary ones deletable once superseded — durable decisions still filed in the wiki). **Done this session:** relocated the hub + engines intact via `git mv` (engines → `docs/preliminary-tests/engines/`, hub → `docs/preliminary-tests/index.html`; every internal link preserved — engine folders untouched/byte-frozen, since each engine's `../../index.html` back-link now resolves to the moved hub and the hub's `engines/…` links still resolve); wrote a new **`docs/index.html` landing page** (states the vision + three control tiers, upfront about Claude authorship + CC0, links to the preliminary tests); updated the 6 render/ui-smoke harnesses, the 5 `findings-*-engine` pages, `music-representation-and-notation.md`, and all process docs (`CLAUDE.md`, `continue-prompt.md`, `README.md`, `experiments/README.md`) to the new paths; propagated the answers into `comprehensive-site-vision.md`, `project-roadmap.md`, `project-open-questions.md`. **Validation:** `node experiments/tests/run.js` **141/141**; `wiki-lint` **0/98**; `ui-smoke` **19/19** (landing + hub + all five engines play from the new location, 0 console errors); `render-engine` **16/16**; `render-percussion` **43/43**. Historical `docs/engines/` references in dated session logs and `wiki/log.md` were intentionally left as-is (they record where things were at the time). **Sub-decisions (working assumptions, easy to change if Tom prefers):** kept an `engines/` grouping under `preliminary-tests/` (lowest-churn, zero internal-link edits) rather than flattening; made `docs/index.html` a real-but-early landing page now (not a bare placeholder) so the Pages root isn't broken and the engines stay one click away. Details: `logs/sessions/029-2026-07-09.md`.

- **THE LONG-TERM GOAL IS DEFINED (Tom, 2026-07-09) + 26-page wiki expansion (session 028).** Tom's directive (preserved verbatim in `logs/sessions/028-2026-07-09.md`, distilled in **`wiki/comprehensive-site-vision.md`**): one **self-contained site** that composes and performs instrumental music in a **wide variety of styles and forms** — some based on pre-established styles documented in the wiki, others **original, created on the fly** from high-order principles; music that is **pleasing, inspiring, and interesting to human listeners** (psychology stays central); **three control tiers** (Start: a few familiar genre categories, seed picks among them; Intermediate: ~20 familiar controls; Advanced: wide, incl. original invented parameters — specifics proposed by Claude, reviewed by Tom over multiple sessions); **seeded determinism with a compact URL suffix** (same full URL = same piece; more compact than today's hash; seed copyable); a **large set of original, clearly annotated shared JS libraries** (no outside code); **everything in one folder** with organized subfolders. Roadmap Phase 3 redrawn around it (3a wiki expansion ✅ this session → 3b design docs → 3c build → 3d improvement loop). Four working-assumption questions filed for Tom in `project-open-questions.md` (engines stay live; site under `docs/`; ~6–10 Start genres; async design review). **The expansion (Tom's prerequisite):** 25 research subagents (Opus 4.8 ×7 hard-synthesis pages, Sonnet 5 ×18 — per Tom's model instruction; Fable 5 orchestrating) + 1 orchestrator-written page added **26 new `draft` pages**: the **design pillar** (style-invention-and-style-space · hybridization-and-fusion · meta-composition-and-style-machines · control-surfaces-and-user-parameters · music-representation-and-notation), **theory/craft** (texture-and-density · modal-and-nonfunctional-harmony · ornamentation-and-embellishment · thematic-development-and-variation · beginnings-endings-and-transitions · tempo-duration-and-pacing · instrument-acoustics-and-idiom), **psychology** (memory-earworms-and-catchiness), **world traditions** (latin-american · reggae-and-caribbean · arabic-and-middle-eastern · sub-saharan-african · european-folk-and-dance · southeast-asian), **popular/art genres** (rock-and-pop · blues-country-and-roots · funk-soul-and-rnb · hip-hop-and-beat-making · contemporary-art-music · medieval-and-renaissance-music). All conventions-compliant with real web citations; `wiki-lint` **0 problems across 98 pages**; index gained a "Designing the comprehensive site" group + reading path; long-standing coverage gaps (Latin American, European folk, Arabic maqam, representation) closed. **Review depth honesty:** orchestrator fully read the design pillar + modal-harmony + texture + thematic-development; the rest got structural review (lint + agent self-reports + spot checks) — claim-level verification is the next queue item, with each agent's self-flagged risky claims listed in its completion report (summarized in `logs/sessions/028-2026-07-09.md`). Notable agent finding to reconcile: all five engines emit `freq` in Performance events while `engine-architecture.md` documents `freqHz` (plus undocumented `beat`/`midi`/`tags`/`weight?`/`expr?` carry-through) — small doc update queued. Details: `logs/sessions/028-2026-07-09.md`.

- **Engine 05 (percussion) → v0.2.0 — sliding scales + 4 more voices (session 027):** Tom liked v0.1 ("the sounds are great") and asked for (1) more variation/variety in genres and development — *sliding scales along many parameters* so the seed (and optionally the user) reaches many structures/timelines, not just three genres — and (2) four more instrument sounds of different types, with selection/combination varied by seed (and optionally user). **(1)** Replaced the three hard-coded style functions (each with one fixed six-section arc) with a single **parametric generator** driven by a **seed-sampled recipe** over continuous/categorical axes: **development** (cyclic ↔ through-composed), **form type** (cyclic/additive/cellDev/throughComposed/process/moment), an intensity-**arc** from **nine** shapes (rise/arch/late-arch/terraced/waves/jo-ha-kyū/front-loaded/level/swell-&-cut), **timeline** (clave/bell/Euclidean/**sieve**/pulse/none), **density**, **looseness**, **lead**, **call-response**, **interlock**, **pitched**. One unified generator realizes any recipe; the three genres are now **presets**, **`auto`** (the new default) samples the whole space, and **every axis is user-overridable** (each UI control defaults to "auto = seed decides"). Over 24 auto seeds: 8 distinct arcs, 5 timelines, all 6 form types, **24 distinct ensembles**. **(2)** Four new original voices in `synth.js`, each a different type: **`clap`** (hand-clap/stick impact), **`scrape`** (guiro/cabasa rasp), **`chime`** (pitched ringing metal — steelpan/hang), **`friction`** (cuíca pitch-bend) → **11 voices**; a new **ensemble sampler** varies the instrument combination by seed (all-metal / skins-&-wood / hands-&-shakers / minimal / …), with a user **Ensemble** preset override. **Additive/engine-05-local** (4 new voices additive to `synth.js`; engines 01–04 keep untouched vendored copies → byte-for-byte unaffected, regression gates confirm). `node experiments/tests/run.js` → **141 passing** (+/reworked to 18 percussion tests: recipe determinism, variety spread, new-voice reachability, per-axis overrides, ensemble-preset constraints, subordinate melody); `render-percussion.mjs` → **43/43** across 8 varied cases incl. all 4 ensemble presets + a dry stress (peak ≤ 0.73, maxStep ≤ 0.14; a palette-coverage gate confirms all 12 voices render); `ui-smoke.mjs` **17/17**; regression `render-groove`/`render-cantabile` unchanged; `wiki-lint` 0. `findings-percussion-engine.md` extended (v0.2 section); `percussion-sound-design.md` palette table + the 4 voices. **Awaiting Tom's ear:** do the auto seeds feel genuinely varied (arcs, timelines, ensembles); do the 4 new voices read as their types; are the default axis ranges tasteful. Details: `logs/sessions/027-2026-07-09.md`.

- **Engine 05 (percussion) shipped — the percussion-centric engine (session 026):** Tom's brief was music from formal concert percussion, through folk ensembles, to informal drum circles; original percussion voices with adjustable timbre/attack/fade; default 2/4 or 4/4 in 4/8/16-bar groups with other meters selectable; each piece with a form; an optional melodic component that only accompanies. **Wiki first** (the brief asked): a survey + two deep-research agents found the wiki strong on rhythm-*form* and drum-machine synthesis but **empty** on concert percussion-ensemble repertoire/forms, drum-circle structure, folk ensembles beyond West African/gamelan, and membrane/gong/cymbal/shaker acoustics — so two new pages were written first: **`wiki/percussion-music.md`** (structure across the three worlds — Varèse/Cage/Reich/Xenakis concert forms, rumba/samba/Arabic/taiko folk ensembles, drum-circle entrainment; the density-arc-carries-form-without-harmony principle, the timeline-vs-lead polarity, meter, fills, melody-as-accompaniment, Euclidean/sieve/stochastic generators) and **`wiki/percussion-sound-design.md`** (original synthesis of the palette — pitch-drop membranes with inharmonic 2-D modes, modal banks for wood/metal/bell, shaped-noise gongs/shakers; the six adjustable macros; ensemble mix rules). **The engine — `percussion@0.1.0`** (`docs/engines/05-percussion/`): seven **original** voices in `synth.js` on three reusable generators (pitch-drop membrane `boom`/`drum` with bass/tone/slap tags, modal bank `wood`/`metal`/`mallet`, shaped noise `gong`/`shaker`), each reading a `note.p` params object; a **three-style meter-parametric composer** (`ensemble` = cell developed over a density arc to a metallic climax + gong ending; `folk` = timeline + interlocking Euclidean ostinati + improvising lead, cyclic; `circle` = heartbeat + rumble swell + stop-cut + fade), with nine meters (defaults **2/4, 4/4**; 3/4, 6/8, 12/8, aksak 5/8/7/8, 5/4, 7/4) and bars snapping to the 4/8/16 phrase group; a **performer** with structured feel (fixed laid-back offsets, style-scaled AR(1) residual, duple-only swing), **per-section accel/rit** (concert ritardando ending, circle rumble accelerando), and the four **timbre macros** (Tune/Ring/Attack/Brightness) folded into every note; and an **optional subordinate mallet+bass ostinato**. **Additive-only** synth/fx change (7 voices + 2 helpers + 4 percussion buses), re-vendored **only** into engine 05 → **engines 01–04 byte-for-byte unaffected**. `node experiments/tests/run.js` → **138 passing** (+15); `render-percussion.mjs` → **31/31** across 3 styles × 5 meters (peak ≤ 0.73, maxStep ≤ 0.11, RMS −17.8..−25.5 dBFS, 0 errors); `ui-smoke.mjs` → **17/17** (all five engines + hub play live); `wiki-lint.mjs` → **0 across 72 pages** (+3). New pages `percussion-music.md`, `percussion-sound-design.md`, `findings-percussion-engine.md`; hub updated. **Awaiting Tom's ear** (open questions in the findings page): do the voices read as their families; are the three worlds distinct; are the timbre-macro/feel defaults tasteful; keep the generic-folk approach or add named-tradition presets? Details: `logs/sessions/026-2026-07-09.md`.

- **Engine 04 (cantabile) → v0.2.0 — Tom's three-note feedback pass (session 025):** Tom listened to v0.1 ("quite good, along the lines I was thinking of") and named three improvements; addressed all three. **(1) Varied accompaniment** (composer): the v0.1 comp was a metronomic ascending arpeggio of equal-length notes and the bass a bare root-on-1 — replaced with per-seed **comping templates** (broken chord, bass-chord "boom-chick," syncopated stabs, flowing mixed-length arp, sustained pad — chosen per bar from a section set, never the same two bars running), **inter-chord voice leading** (nearest-voicing → a smooth bed, tested < ~5 semitones/bar centroid motion), and a **varied bass** (root/fifth, octave, walking approach tones, anticipations, rhythmic variety). Kept low (comp vel ≈ 0.32 vs leads ≈ 0.65) so it never covers the leads. **(2) More organic, less-synthetic leads** (additive `synth.js` helpers wired into aria/reed/wire/glass): diagnosed *why* oscillator-plus-filter voices read as "a synthesizer" and added the three highest-leverage cures — **`microDrift`** (a few cents of always-on quasi-random pitch wander — a real instrument is alive even played straight; the strongest fix), **`bodyResonance`** (fixed per-voice `peaking`-filter formants that don't track pitch — the "physical body" cue), **`onsetTransient`** (a short per-voice attack noise: bow scratch/breath chiff/pick click — the identifying transient). **(3) Vocal phrasing** (composer + performer): lead lines now split into **breath-delimited, motif-seeded sub-phrases** (2–3 bars, each landing on a resting goal tagged `breath`); the performer realizes each breath as a real **silence** (a lift, not a clip), **blocks portamento across a breath**, adds an **agogic hesitation** entering a phrase, and eases dynamics into the breath. **Additive & engine-04-local** — only the four expressive voices + the cantabile composer/performer changed; shared voices (`bass`/`rhodes`/…) and `fx.js` untouched, so **engines 01–03 are byte-for-byte unaffected** (regression render gates 16/16 ×3 confirm). `node experiments/tests/run.js` → **123 passing** (+5); `render-cantabile.mjs` **25/25** (peak ≤ 0.60, maxStep ≤ 0.064 — both cleaner than v0.1; RMS ≈ −16.7 dBFS); `ui-smoke.mjs` **14/14**; `wiki-lint.mjs` **0/69**. New page `wiki/original-sound-design.md` (Tom-requested synthesis insight); `findings-cantabile-engine.md` extended. **Awaiting Tom's ear** (open questions in the session log + findings page): is the accompaniment varied without stepping on the leads; do the leads now read as instruments-with-a-body rather than synths (drift/body/onset magnitudes tasteful?); do the breaths land in musical places at the right length. Details: `logs/sessions/025-2026-07-09.md`.

- **Engine 04 (cantabile) shipped — the expressive engine (session 024):** Tom's brief was music inspired by small groups whose instruments are played with great **per-note expressivity** (string quartet, jazz sax, rock lead guitar): four original expressive voices, expression tied to compositional structure, original expressivity sliders, and an original genre. Built **`cantabile@0.1.0`** (`docs/engines/04-cantabile/`). **The core gap it fills:** every prior shared-synth voice set its pitch once and could not shape a note after onset. The four new voices — **`aria`** (warm bowed), **`reed`** (breathy/reedy), **`wire`** (electric, blooming grit), **`glass`** (pure/shimmering), all original and imitating no real instrument — take a `note.expr` object and realize **continuous intra-note automation** via three new `synth.js` helpers: `pitchExpr` (scoop/portamento/release-bend on additive `detune` + a delayed, blooming vibrato), `exprEnv` (a swelling *messa di voce* amplitude envelope; all ramps to defined endpoints + a true-zero tail), and `breathLayer`/`panTo` (bow/breath grain + chamber stereo seating). **Expression is structural, not random** (the [expressive-performance](../wiki/expressive-performance.md) mandate): the new composer `experiments/composers/expressive-chamber.js` builds a modal **dramatic arc** (intro · theme · dialogue · rise · climax · return · coda; six lyrical modes; non-functional/plagal harmony; a germ motif developed across the arc; a real held modal-tonic ending) and stamps each note with a structural **weight (0..1)**; the performer `experiments/engines/cantabile/engine.js` computes `drive = Expression × weight × (0.5 + 0.7·Ardor·sectionIntensity)` and scales every gesture by it — so the **climax carries ~3–4× the intro's vibrato depth and higher brightness** (asserted by test). **Original sliders** (the brief asked for them): master **Expression** + **Song** (vocal pitch inflection), **Bloom** (how a held note develops), **Ardor** (dramatic intensification tied to the arc), **Rubato** (timing breath), **Grain** (air/texture). The ensemble (which of the four leads/answers/inner) **varies with the seed and the user's selection** — all four lead on some seed (asserted). **Additive-only** change to `synth.js` (5 voices + helpers) — `fx.js` untouched; the expressive voices route through the existing melody bus and pan in-voice, so **engines 01–03 are byte-for-byte unaffected** (their vendored `synth.js` has none of the new voices; their render gates still 16/16). `node experiments/tests/run.js` → **118 passing** (+13); `render-cantabile.mjs` **25/25** (peak ≤ 0.63, maxStep ≤ 0.09 — cleaner than Engine 01, RMS ≈ −16 dBFS, incl. a maximum-expression stress case); `ui-smoke.mjs` **14/14** (all four engines + hub play live, 0 errors); `wiki-lint` **0 across 68 pages**. New page `wiki/findings-cantabile-engine.md`; hub updated. **Awaiting Tom's ear** (open questions in the findings page): do the four voices read as distinct-and-expressive; are the default slider magnitudes tasteful (scoops singing vs seasick); does the call-and-response read as dialogue; is the held ending the right length. Details: `logs/sessions/024-2026-07-08.md`.

- **Engine 02 static — texture DENSITY (crossfade + bell clusters), not the synth (session 023):** after v0.1.1, Tom re-listened to Engine 02 (ambient-drift) and still heard "static in the middle of sustained notes," with an exact deterministic repro (`seed=drift-1`, warm/D3, `dur=150`, `rev=0.5`, `vol=0.62`) and timestamps (ranges ~00:42–45 and ~01:37–43; instants ~01:59, 02:09). A headless **OfflineAudioContext** render reproduces the exact signal — **confirmed against Tom's 24-bit/48 kHz WAV**: offline peak 0.6193 / RMS −13.3 dBFS match the recording's 0.6195 / −13.4 to 3–4 sig figs. Diagnosis (per-voice + reverb-on/off isolation; an amplitude-envelope **modulation-spectrum roughness** metric; pure active-note analysis) **ruled out** the leading hypotheses: reverb adds ≈ 0 to full-mix roughness (dry ≈ wet); no clicks (`maxStep` ≈ 0.10); per-voice synth tweaks (darker pad, softer/shorter bell) did **not** help. The static is **sensory roughness** (partials beating within a critical band — `wiki/auditory-perception-basics.md`, Plomp & Levelt: "a synth pad exposes tuning a fast line would hide") at **two structural moments**: (1) region **crossfades**, where the launch composer held two full pad voicings at once for an 8 s overlap, and (2) dense **bell clusters** of up to five long inharmonic bells. **Fix is in the composer** (`experiments/composers/ambient-drift.js`, ambient-only; `synth.js`/`fx.js` unchanged, so tonal/groove untouched): a true pad crossfade (pad overlap **8 s → 4 s**; drone stays 8 s for continuous bass) + a deterministic **bell polyphony cap (≤ 4)** (focal always kept) + a mild ring trim. Offline, both flagged windows drop to the clean control level (−6 dB crossfade, −4 dB cluster); spectrograms show the beating "grid" collapse to clean harmonics; level/peak/click-safety unchanged. **ambient-drift → 0.1.2.** 105 tests; render-ambient 16/16; ui-smoke 11/11; wiki-lint 0/67. Different cause/cure from Engine 01's envelope puff — the "static" label covered two unrelated mechanisms. Details: `logs/sessions/023-2026-07-08.md`.
  - **⚠️ OUTCOME (Tom re-listened, 2026-07-08): the static PERSISTS on v0.1.2.** So the crossfade/bell-density fix — although it measurably cleaned the two flagged windows offline — was **not** the (whole) thing Tom hears. Tom's takeaway question: is the residual static **engine-specific or in the shared libraries?** **Assessment (informed, not yet ear-confirmed): most likely the SHARED libraries — the *sound* of the `synth.js` `pad`/`bell` voices (the pad is a harmonically-rich detuned-triangle ensemble that beats internally; the bell is inharmonic and clangs) and/or the long `fx.js` reverb — EXPOSED by Engine 02's quiet/sparse/sustained ambient texture, rather than logic unique to Engine 02's composer.** Strongest evidence: reducing the Engine-02 *arrangement* density (v0.1.2) did not clear it for the ear → the residual is in the shared *sound generation*, not the arrangement. Corroboration: the "static" was also reported on Engine 01, and session 022 traced that to the shared `synth.js` envelope — the shared synth has been implicated twice. **Decisive test for next time (harness exists):** audition a SINGLE sustained note from each shared voice (`pad`/`bell`/`drone`) in isolation, dry and with reverb — if a lone pad/bell note sounds "static-y" on its own, it is definitively shared-library; if only Engine-02 stacking triggers it, it is arrangement. **Confound to rule out first:** confirm Tom actually heard *true v0.1.2* (GitHub Pages may have served a cached v0.1.1 — the page footer/badge shows the version). **Per Tom (2026-07-08): DE-PRIORITIZE further Engine-02 static work; next few sessions focus on composition + building more engines for various music types (see queue).** Return to the static later with the isolation test above.
- **The real static fix — the synth ENVELOPE, not the reverb (session 022):** after v0.3, Tom re-listened and reported the "static" *still there*, with an exact repro — "the extended note at the end of the first two phrases," **the same in Chrome, Safari, and Firefox** (`seed=d8d8d8`). Cross-browser + same-place = deterministic, in the signal (not real-time, not Safari). Bisected offline: melody-only renders ruled out reverb and vibrato; a zoom of the **dry keys note** revealed a **broadband "puff" ~0.55 s in** (a 2.3× amplitude bump) at the envelope **release onset**. Root cause: the shared `synth.js` `envGain` held the gain **flat** at the peak then switched **abruptly to a fast release** — a slope discontinuity that splatters a broadband burst, landing mid-note on long notes. Fix: never hold flat — struck voices (`keys`/`rhodes`) decay in one continuous exponential curve (no mid-note corner); sustained voices glide to a sustain level then release. **Exponential ramps only** so it renders identically in every browser (Tom uses Safari). Spectrogram confirms the puff is gone. Versions → **tonal 0.3.1, groove 0.2.1, ambient 0.1.1**. 105 tests; render gates 16/16 ×3 (tonal RMS ~−16 dBFS now that the melody decays like a struck EP — still in band); ui-smoke 11/11; wiki-lint 0/67. This is the cause the v0.2/v0.3 reverb hypotheses were circling; the reverb-grain + contour-variety work from session 021 stands on its own. **Next: Tom's listen on v0.3.1 with `seed=d8d8d8` to confirm the static is finally gone.** Details: `logs/sessions/022-2026-07-08.md`.
- **Second listener-feedback pass on all three engines (session 021):** Tom sent feedback JSONs for all three. **(1) The "static in the middle of sustained notes"** (Engine 01 v0.2 *and* Engine 02) was diagnosed with headless OfflineAudioContext probes (per-voice spectra, aliasing/click/HF-burst detectors, canvas spectrograms) — the renders are **clean** apart from **reverb-tail grain**. Fixed at the source in the shared `fx.js` `makeIR`: a **near-Gaussian** noise IR (Irwin-Hall, energy-normalized) cut the wet-tail energy coefficient-of-variation **0.49 → 0.33 (−31%)** with brightness/loudness unchanged — and a control probe showed this is *not* re-darkening (a third lowpass pole *raised* grain), which is why v0.2's darkening hadn't worked. Since offline is otherwise clean, any residual static is a **real-time underrun**, so all three engines now open the live `AudioContext` with **`latencyHint: 'playback'`** (the documented crackle cure). **(2) Engine 01 → v0.3.0:** every seed shared one melodic contour (rise to a late apex); `composePiece` now varies the **contour archetype per piece** (arch / arch-late / descending / ascending / wave, weighted to Huron's dominant types, contrasting shape for B, per-archetype start register) — openings across 200 seeds are ~50/24/26% up/flat/down and the apex lands early/mid/late; `composePeriod` and its 15 tests are untouched. **(3) Engine 03 → v0.2.0:** the comp that "sounded the same every seed and overpowered the drone" is fixed with **voice leading** (consecutive-chord centroid motion ≈ 1.3 semitones = a stable bed, not a jumping melody), a per-seed **progression bank + comping rhythm**, and a soft **sustained root+fifth pad drone** under a softer, legato comp. New live-playback gate `experiments/tools/ui-smoke.mjs` (hub + 3 engines play, 0 console errors). Validation: `node experiments/tests/run.js` → **105 passing**; `render-engine`/`render-ambient`/`render-groove` **16/16 each**; `ui-smoke` **11/11**; `wiki-lint` **0 problems across 67 pages**. Findings pages updated (tonal § v0.3, ambient grain note, groove § v0.2). **Honest caveat:** I could not reproduce a gross static headlessly — the grain fix + two variety fixes are offline-verified, but the residual-static hypothesis (real-time underruns) is device/browser-dependent and needs Tom's ear (and, if it persists, his browser/device) to confirm. Details: `logs/sessions/021-2026-07-08.md`.
- **Engine 03 (groove-lofi) shipped + hub made compact/collapsible (session 020):** Tom asked to build Engine 03 as a groove/lo-fi engine and to make the hub's engine list compact, descending (newest first), with each description a collapsed box shut by default. **(1) Engine 03 — `groove-lofi@0.1.0`** (`docs/engines/03-groove-lofi/`), the trio's groove member and the project's **first engine with drums**: a looped lo-fi hip-hop beat realizing `wiki/groove-and-embodiment.md`'s targets — backbeat snare on 2 & 4, a medium-syncopation kick that always keeps beat 1 (the Witek inverted-U peak), ghost notes (≈0.22) and a sparse bell lead in the B half, a velocity hierarchy (**not** timing noise), warm 7th/9th Rhodes chords, a weighty bass, and a vinyl-crackle bed. The load-bearing decision: **swing (a per-beat phase warp) and the laid-back backbeat (a fixed ~18 ms per-instrument offset) are the performer's structured job over a perfectly quantized composer grid** — the concrete implementation of the page's "structured-not-random microtiming" correction. Added four voices to the shared `synth.js` (`kick`/`snare`/`hat` from `synthesis-recipes.md` + a warm `rhodes` EP; drums share a deterministic cached noise buffer) and drum/rhodes buses to `fx.js` (kick dry+full-range, drums lightly sent), re-vendored into all three engines. Full state in the URL, self-report table, piano-roll with drum lanes, JSON feedback. **(2) Hub `docs/index.html`** rebuilt: engines listed 03→02→01, each a one-line head (number · title · tagline · Play) with an "About this engine" `<details>` **collapsed by default**; denser padding; the dashed "coming soon" card replaced by one muted line. `node experiments/tests/run.js` → **105 passing** (+10 groove); `render-engine.mjs` 16/16, `render-ambient.mjs` 16/16, `render-groove.mjs` 16/16 (percussive `maxStep` bound); headless UI smoke → hub + all three engines load & play with 0 console errors; `wiki-lint.mjs` → 0 problems across 67 pages. New page `wiki/findings-groove-lofi-engine.md`. Details: `logs/sessions/020-2026-07-08.md`.
- **Engine 01 → v0.2 (feedback pass) + Engine 02 shipped (session 019):** Tom listened to tonal-classical v0.1 (rated 3/5) and asked for a new version fixing his feedback, then a contrasting second engine. **(1) Engine 01 v0.2:** diagnosed the reported "faint static/clipping on the final note" as a bright/noisy convolution-reverb wash + glue-compressor pumping (not master clipping — peak was ≈ 0.58) and fixed it (darker/smoother IR, band-limited reverb return, gentler glue, true-zero envelope tails; tail probe now 0.0 dB post-peak swell, HF-fizz ≈ 0.00); replaced the mechanical per-note white jitter with a real expressive layer per `wiki/expressive-performance.md` (nested phrase-arch tempo + sqrt ritardando, dynamic arch, high-loud, structured articulation, chord roll, delayed vibrato, correlated AR(1) residual, lag-1 autocorr ≈ 0.8); raised default tempo 92 → 110 BPM. **(2) Engine 02 — `ambient-drift@0.1.0`** (`docs/engines/02-ambient-drift/`, hub card added): the deliberate contrast — Eno's *Music for Airports* incommensurable-loop recipe (five mutually-prime-ish periods 17.5/23/28.5/32.1/40 s, earliest resync > 190 s) over a slow modal drift, four palettes, reusing the shared synth/fx extended with new `bell`/`pad`/`drone` voices + a parameterizable dark reverb; a deliberately minimal ambient performer (pace, correlated residual, global fade in/out — no meter). `node experiments/tests/run.js` → **95 passing** (+7 ambient); `render-engine.mjs` → 16/16, `render-ambient.mjs` → 16/16; headless UI smoke test → both engines + hub load with 0 console errors; `wiki-lint.mjs` → 0. New page `wiki/findings-ambient-drift-engine.md`; `findings-tonal-classical-engine.md` extended with the v0.2 pass. Details: `logs/sessions/019-2026-07-08.md`.
- **Phase 2 launched — Engine 01 (tonal-classical) shipped (session 018):** Tom opened Phase 2 and asked for the launch engine: extend the composer from one period to a whole piece (endings, motivic variation, full form) and build the audible synth/fx layer. Done and validated. **`tonal-classical@0.1.0` is live at `docs/engines/01-tonal-classical/`**, with a hub `docs/index.html` cataloging it as **Engine 01** (numbered for reference; future engines slot in). (1) **Whole-piece composer**: extended `experiments/composers/tonal-phrase.js` with `composePiece()` — an **intro + rounded ternary (A B A′) + coda** (30 bars). A′ **restates A's harmony and on-beat melody but ornamented** (motivic variation by diminution); **B contrasts** by lower register + off-tonic opening (vi/iv) + a dominant re-approach; the **coda is a real ending** (R6): harmonic rhythm broadens then accelerates into a mid-bar ii–V, resolving to a lengthened final tonic, with a melodic 3̂–2̂–1̂ close and a performer ritardando. (2) **Audible layer** (shared-libraries build-order item 2): new `experiments/lib/synth.js` (FM electric-piano lead, detuned-triangle strings, sine+triangle bass — click-safe) + `experiments/lib/fx.js` (per-voice buses with non-bass HP and mono lows, one synthesized-IR convolution reverb, saturation → glue → limiter master chain). (3) **Performer + scheduler**: `experiments/engines/tonal-classical/engine.js` (pure `renderPlan` for beats→seconds/dynamics/ritard; lookahead scheduler for live play; offline render for validation). (4) **UI**: two-level controls, editable seed, **full state in the URL hash** (shareable), self-report table, piano-roll visualization with playhead, and a **download-JSON feedback** affordance. Everything vendored into the docs engine folder (self-contained, `file://`-runnable). Validation: `node experiments/tests/run.js` → **88 passing** (+8 piece, +6 engine); `node experiments/tools/render-engine.mjs` → **16/16 gates** (no clip, no gross discontinuity, no gap, ≈ −11 dBFS across three seeds/modes); `wiki-lint.mjs` → 0 problems across **65 pages** (+1 findings page). New page `wiki/findings-tonal-classical-engine.md`; Phase-2 flip propagated to `CLAUDE.md`, `continue-prompt.md`, `README.md`, `project-roadmap.md`, `shared-libraries.md`, `project-open-questions.md` (R6 addressed, R3 now validatable). **GitHub Pages still needs enabling by Tom** (repo Settings → Pages → deploy from `main`/`docs/` — one-time). Details: `logs/sessions/018-2026-07-08.md`.
- **Phase-2 preparations complete (session 017):** the two remaining blockers before engine-building are done, per Tom's session-prompt request. (1) **Worked R1/R2 composer** — `experiments/composers/tonal-phrase.js` assembles `theory` + `rng` into a phrase-first melody over a goal-directed, cadence-first progression as one eight-bar **parallel period**, emitting the engine-architecture Note-event schema deterministically. Harmony is a **backward Piston walk** (fix the cadence chord, sample predecessors from a reversed Piston table weighted by forward probability — pre-dominant-before-dominant falls out for free); melody is **contour-first** over fixed goal tones (arch, unique apex, chord tones on strong beats, step bias, ~0.7 post-leap reversal, stepwise cadential approach) with the consequent restating the antecedent's basic idea. 15 structural tests verify determinism, cadence goals, restatement, in-key melody, unique apex, step-dominance (~71%/~7%), and interior post-leap reversal (~68%, matching von Hippel & Huron once the deliberate apex/cadence structural leaps are excluded). (2) **OfflineAudioContext render-and-measure harness** — `experiments/lib/analysis.js` (pure PCM metrics, 8 Node tests on synthesized signals) + `experiments/demos/offline-render.html` + `experiments/tools/render-measure.mjs` (Playwright, dev-time only). It validated `transport`'s beats→seconds math as **sample-accurate** (all onsets detected, jitter 0.015 ms, max error 0.24 ms), confirmed the click-safe envelope (maxStep 0.018 vs 0.50 for a hard-gate control), and rendered the composed period clean (no clip/gap, −25 dBFS). `node experiments/tests/run.js` → **74 passing** (was 51: +15 composer, +8 analysis); `node experiments/tools/render-measure.mjs` → 12/12 gates green; `wiki-lint.mjs` → 0 problems across **64 pages** (+1 findings page). R1 and R2 marked *worked* in `project-open-questions.md`; the audio-timing gap that blocked engine-dependence on the foundation is closed. New page `wiki/findings-tonal-phrase-composer.md`; `findings-shared-lib-foundation.md` extended (analysis + harness). Details: `logs/sessions/017-2026-07-08.md`.
- **Shared-library foundation trio complete (session 016):** built `experiments/lib/theory.js` — the third and last foundation module named in `wiki/shared-libraries.md`, after `rng` and `transport` (session 010). It provides note-name/MIDI/frequency conversion, size-only interval naming, 13 named 12-TET scales, Krumhansl-Kessler tonal-hierarchy weights, generic tertian chord construction (triads/sevenths/roman numerals derived from any 7-note scale, not hand-listed per scale), T/S/D functional-harmony labels for major/natural-minor keys, Piston's root-progression table plus rock-corpus chord priors pre-weighted (0.6/0.3/0.1 by tier) for a seeded picker, and cents-based tuning tables (slendro, a rast-like maqam approximation, JI ratios) for non-12-TET traditions — all transcribed from `wiki/harmony.md` and `wiki/tuning-and-scales.md`. Added 26 tests (`node experiments/tests/run.js` → **51 passing**, up from 25) plus a dev-time-only cross-check against Tonal (`@tonaljs/tonal`, installed to a scratch directory outside the repo, never a project dependency) covering all 128 MIDI notes, every scale across all 12 tonics, and major/natural-minor diatonic chords across all 12 tonics: **zero mismatches**. Updated `wiki/findings-shared-lib-foundation.md` (new module + a new "Oracle validation" section resolving `shared-libraries.md`'s open "oracle practice" question) and `wiki/shared-libraries.md` (prototype-status, build-order, and open-questions sections). `node experiments/tools/wiki-lint.mjs` still reports 0 problems across 63 pages. Details: `logs/sessions/016-2026-07-08.md`.
- Wiki instantiated per `wiki/llm-wiki.md` → schema in `wiki/conventions.md`; **63 pages** across all nine categories (theory, psychology, genres, craft, algorithms, implementation, evaluation, project, meta) plus a `findings` page. **All 60 content pages plus the findings page are now `reviewed` (63/63) — the first full verify-and-mature pass over the entire wiki is complete as of session 015.**
- **Last 3 draft pages verified (session 015):** `improvement-loop.md`, `shared-libraries.md`, `indian-popular-and-film-music.md` — all promoted to `reviewed` via three parallel subagents. `improvement-loop.md` had one real drift (a findings-page filename convention, `findings-YYYY-MM-DD-<topic>.md`, that neither `conventions.md` nor the one real findings page actually follows — corrected). `shared-libraries.md` was checked claim-by-claim against the real prototype code (`experiments/lib/rng.js`, `experiments/lib/transport.js`) and tests: fixed an "25 passing assertions" → "25 passing tests" imprecision (propagated the same fix to `findings-shared-lib-foundation.md`) and folded the classic-script/`file://` vendoring constraint into the main vendoring-model section; everything else (namespace, injected-dependency pattern, RNG distributions, consistency with engine-architecture's session-014 wording) held up exactly. `indian-popular-and-film-music.md` got its queued light source-fetching pass: Beaster-Jones/Booth remained inaccessible, but a genuine peer-reviewed source (Dias & Jähnichen 2014) was found and cited for the general Western-chord-over-raga phenomenon (the specific I–vi–IV–V claim stays hedged); the scroll.in Rahman quote was reconstructed near-verbatim from search snippets (site still 403s automated fetches); an overstated Diljit Dosanjh "headliner" claim was corrected; Naatu Naatu/Ilaiyaraaja spot-checks held up. `node experiments/tools/wiki-lint.mjs` still reports 0 problems across 63 pages. Details: `logs/sessions/015-2026-07-07.md`.
- **Doctrine syntheses verified (session 014):** verified generative-music-failure-modes, generative-music-design-patterns, and engine-architecture — all now `reviewed` (three parallel subagents, one per page, checking link integrity, claim-to-target accuracy against the current post-session-013 state of every page cited, and internal consistency; these pages were written in session 001 before eleven domain-verification sessions revised what they cite). failure-modes and design-patterns held up with zero drift — their citations are mechanism/prior-art pointers, not sourced numbers, so the terminology/number corrections from sessions 002–013 never touched them. engine-architecture.md had one real fix: its "Runtime rules" section said engines could use "vanilla ES modules or a single classic script," contradicting the session-010 finding (`findings-shared-lib-foundation.md`) that cross-file ES-module `import` is CORS-blocked under `file://` — rewritten to state classic-script-only vendoring (single- or multi-file via `<script src>`, no cross-file `import`/`export`). `node experiments/tools/wiki-lint.mjs` still reports 0 problems across 63 pages. Details: `logs/sessions/014-2026-07-07.md`.
- **Cross-domain consistency lint, first full pass (session 013):** ran the long-queued contradiction/duplication sweep across all 54 `reviewed` pages — four parallel subagents, one per cluster (theory+craft, psychology, genres, algorithms+implementation+evaluation), each checking internal consistency and comparing tempo/BPM, consonance/dissonance, and rhythm-category claims (the topics flagged as likely trouble spots) both within and, via the main session, across clusters. Found and fixed four real issues: (1) auditory-perception-basics.md claimed octave equivalence is "near-universal," citing musical-universals.md — which actually documents it as contested (Jacoby et al. 2019 Tsimane' data); softened and corrected the cross-reference. (2) A three-way swing-ratio-by-tempo mismatch across rhythm-and-meter.md, expressive-performance.md, and jazz-and-improvisation.md (the last also self-contradictory) — all three cited Friberg & Sundström (2002) but gave different numbers at the same tempo; since the paper's exact table isn't reproducible from open sources, jazz-and-improvisation.md is now the single canonical statement (verified anchors: ~3.5:1 at slow tempi, ~1:1 by ~300 BPM, ~100 ms constant short note), with a new verified nuance added (drummers run above 2:1 at medium tempi, soloists below). (3) web-audio-fundamentals.md's "≥3–10 ms" gain-ramp click-avoidance floor conflicted with synthesis-recipes.md's 1–3 ms percussive attacks — clarified as an accepted exception (any nonzero ramp already kills the click). (4) A minor ≤3 vs ≤3–4 voice-count-cap discrepancy (counterpoint-and-voice-leading.md vs composition-craft.md) — clarified via cross-reference as answering different questions, not changed. Several suspected duplications (Satie anecdote, Reich critique, CHORAL, Xenakis works) were checked and found consistent, not contradictory — left as-is per the wiki's hub-and-spoke linking convention. `node experiments/tools/wiki-lint.mjs` still reports 0 problems across 63 pages. Details: `logs/sessions/013-2026-07-07.md`.
- **Psychology domain complete (session 012):** verified the last 2 psychology-domain drafts — attention-and-background-listening, musical-universals — both now `reviewed` (two parallel verification subagents fetching PMC/DOI/publisher sources directly). Real fixes: a wrong co-author on a serial-recall citation ("Perham & Currie 2011" → actually **Perham & Vizard 2011**; Currie is a real Perham co-author but on an unrelated 2014 paper, now added alongside it); a fully fabricated author name on the 45-vs-65-dB white-noise study ("Onur Yorulmaz" → actually **Awada, Becerik-Gerber, Lucas & Roll 2022**); a missing citation (Thompson, Schellenberg & Husain 2001, discussed in-body but absent from Sources); an unsupported claim that Woods et al. (2024) shows *irregular* AM modulation is extra-distracting, softened since that study tested only regular 16 Hz modulation; a Mehr et al. (2019) figure corrected from "music in all 315 societies" to the paper's actual "309 of 315"; and a conflated-claims fix separating Savage et al. (2015)'s 18 individually-universal features from their separate ~10-feature co-occurrence network (percussion/dance travel together but aren't each independently universal in the same sense). Details: `logs/sessions/012-2026-07-07.md`.
- **Theory domain complete (session 011):** verified the last 5 theory-domain drafts — counterpoint-and-voice-leading, rhythm-and-meter, groove-and-embodiment, tuning-and-scales, timbre-and-orchestration — all now `reviewed` (five parallel verification subagents, each fetching primary sources directly — PDF text extraction for Huron 2001/Witek 2014/McAdams 1995&2019/Fitch & Rosenfeld 2007 rather than trusting secondary paraphrases). Real fixes: a wrong-author citation ("Senn et al. 2022" → actually Stupacher, Wrede & Vuust 2022) plus a new caveat that a 2024/25 study (Senn's group, Witek herself a co-author) found the syncopation inverted-U did *not* replicate with a fresh stimulus set (p=.834) — the page now flags the curve as best-default-but-contested rather than settled; a dropped unsupported "brainstem" claim (Hove et al. 2014 supports only a cochlear basis); a corrected Moelants (2002) preferred-tempo center (100–120 BPM → ~120–130, since the paper argues for raising the older estimate); "pulse reversal" replaced with Fitch & Rosenfeld's actual term "resetting" throughout rhythm-and-meter and groove-and-embodiment; a misattributed "reed organ" demonstration re-sourced to Saldanha & Corso (1964) + the Fassett "Strange to Your Ears" recording instead of the McAdams review PDFs (which don't contain it); a softened brightness→valence claim (the two cited sources disagree on direction); a re-sourced pseudo-polyphony citation (Gray & Bunescu 2020 only name-drops the term; re-cited to Bregman's streaming account via Huron). tuning-and-scales held up almost perfectly — every computable cents/comma value and the Krumhansl & Kessler (1982) profiles matched to hand-computed/canonical precision. Details: `logs/sessions/011-2026-07-07.md`.
- **Evaluation domain + shared-library foundation (session 010):** completed the queued **evaluation** verify-and-mature pass — evaluation-challenges, listening-tests-and-feedback, computational-music-metrics, corpus-analysis all `reviewed` (four parallel verification subagents). The domain held up well; the substantive fixes were a wrong Farbood slider sampling rate (10 → 60 Hz, matching the cited 2012 paper) and an unsourced Essen corpus size (~10,000 → the sourced ~4,900 subset + noted secondary totals), plus a Foote novelty-kernel citation split and a MUSHRA-anchor precision reword. Also **started the shared-library groundwork (queue item 8):** prototyped and headless-tested the two cheapest foundation modules in `experiments/lib/` — `rng` (seeded mulberry32 PRNG, named streams, distributions incl. 1/f pink noise, Euclidean/Bjorklund rhythm) and `transport` (`MusicClock` + lookahead `Scheduler`). `node experiments/tests/run.js` → 25 passing assertions (incl. an exhaustive maximal-evenness proof of Euclidean output for all E(k,n), 1≤k≤n≤32). Finding: libraries must be **dual-format (UMD-lite) classic scripts**, not ES modules, because cross-file `import` is CORS-blocked under `file://`. Details in `wiki/findings-shared-lib-foundation.md`; `logs/sessions/010-2026-07-07.md`.
- **Policy revisions + new page + craft domain (session 009):** Tom revised two standing decisions — (1) the **dependency policy** now prefers Claude's *own original, first-party libraries* over vendored third-party helpers when shared code is genuinely needed (still no absolute ban, still runtime dependency-free by default), and (2) the **license** is now public-domain CC0 for *both* code and prose (single `LICENSE`; the separate `LICENSE-wiki` removed). Both propagated across the wiki + process docs. New page **`shared-libraries.md`** (`draft`) seeds the research Tom asked to start: the plan for the project's original libraries (`theory`/`transport`/`rng`/`synth`/`fx`/`analysis`) and the vendoring model that keeps engines self-contained. Also completed the queued **craft** verify-and-mature pass — composition-craft, expressive-performance, what-makes-music-good all `reviewed` (three parallel verification subagents); expressive-performance took the most fixes (KTH k-spread stated as ">2.5×" vs the source's "factor above 2, at most 2.5×"; an unsourced ≈8% dotted-sixteenth figure → KTH 5% default; the Datseris laid-back offset overstated at ~25 ms vs the paper's ~10 ms mean; Prame vibrato per-singer range unsupported). Details: `logs/sessions/009-2026-07-07.md`.
- **Genres pages, part 2 (`reviewed`, session 008 — completes the genres domain):** film-and-game-music, gamelan, indian-classical-music, west-african-rhythm, east-asian-traditions — verified via five parallel verification subagents fetching Wikipedia/Britannica/UNESCO/MTO/industry-interview sources directly. indian-classical-music held up entirely (all 12 checked claims confirmed, including resolving its own open question on tintal's khali placement at beat 9). Real fixes: a Red Dead Redemption music claim (A minor/130 BPM) was true but cited to a source that never mentions the game — replaced with the actual composer-interview citation (film-and-game-music); an invented-looking specific ombak Hz example replaced with the real documented beat rates, and kotekan's "4–8×" density corrected to "four or eight times" (two discrete multipliers, not a range) (gamelan); three quote-fidelity fixes — a Novotney quote's hedged "most typical" had been swapped for "all," an Anku quote fabricated the word "known" and stitched on an unquoted paraphrase, and an Agawu quote dropped "and meter" (west-african-rhythm); an overstated "3,000 years of documented practice" guqin claim softened, a missing academic citation added for the Chinese tonggong mode-rotation system, and an unsourced "oldest continuously performed" superlative about Sujecheon flagged as the page's own characterization (east-asian-traditions). Details: `logs/sessions/008-2026-07-07.md`.
- **Genres pages, part 1 (`reviewed`, session 007):** style-and-genre-overview, western-classical-tradition, jazz-and-improvisation, minimalism-and-process-music, ambient-and-generative-genre, electronic-and-dance — verified via six parallel verification subagents fetching Wikipedia/arXiv/MTO/Dancecult/ASA/publisher sources directly. Real fixes: a Frieler et al. (2017) corpus-size error conflating Owens' 250-transcription corpus with Frieler's own 56-transcription one (jazz); a publisher misattribution for Meyer's *Style and Music* and an unsupported direct quote from a Brackett review (style-and-genre-overview); a mis-cited Satie 1920-premiere anecdote and a Koan-rules misquote ("about a hundred and fifty" vs. the real "a hundred and fifty") (ambient); a Solberg (2014) attribution overreach (two techniques not in her paper, one of them actually Robert Smith's term) plus an unsupported downtempo BPM range corrected from 60–100 to 70–110 (electronic-and-dance); a hedge added to an "Algo Pärt" claim about deriving the M-voice from text syllable counts, which the paper's abstract doesn't state (minimalism). western-classical-tradition.md held up almost entirely (several exact-quote checks confirmed verbatim); one precision citation added (Caplin 1998 for the 2+2+4 sentence bar-count). Details: `logs/sessions/007-2026-07-07.md`.
- **Implementation pages (`reviewed`, session 006):** web-audio-fundamentals, scheduling-and-timing, synthesis-recipes, effects-and-mixing, audio-worklets-and-performance, javascript-music-libraries — verified via six parallel verification subagents fetching MDN/W3C/spec/GitHub/npm-registry sources directly (this domain is code/API-heavy, so the pass leaned on primary technical sources rather than paper-fetching). Real fixes: a wrong mode-ratio number (xylophone's third partial listed as ~9, which is actually marimba's — corrected to ~6); an overstated float32-timestamp-precision claim (corrected with real node-computed numbers: ~0.24ms at 1h, growing linearly to ~4ms at 10h); a Freeverb citation that implicitly attributed the classic 0.84/0.2 feedback/damp defaults to the cited STK port, which actually ships its own altered 0.91/0.1 defaults (now both stated); and an overstated Blob-URL-AudioWorklet-under-file:// browser-compat claim whose cited source didn't actually establish the compat matrix asserted (reworded as unsettled, open question widened from Safari-only to all three browsers). Two minor softenings (BS.1770 shelf-corner frequency, "exponentially decaying" → "decaying" for a power-law IR envelope). The `javascript-music-libraries.md` survey's version/license/date/size claims for ~11 libraries were checked against live npm registry data and all held up exactly. Details: `logs/sessions/006-2026-07-06.md`.
- Project/meta pages `reviewed`: project-mission, project-roadmap, project-open-questions, previous-experiments-lessons, conventions. **Corrected 2026-07-07 (session 010):** this line previously also listed generative-music-failure-modes, generative-music-design-patterns, engine-architecture, and improvement-loop as reviewed, but their frontmatter is `draft` (they were never in a verify sweep) — a review-status bookkeeping drift, now reconciled to the page frontmatter (which `index.md` already reflected).
- **Theory-core pages (`reviewed`, session 003):** melody, phrase-structure, form-and-structure, tension-and-release, harmony — verified against primary sources via five parallel verification subagents; real fixes to the tonal-interval-space tension weights and the Axis-progression count, plus many citation upgrades.
- **Psychology pages (`reviewed`, session 004):** musical-expectation, repetition-and-familiarity, emotion-and-meaning, pleasure-and-reward, complexity-and-preference, auditory-perception-basics — verified via six parallel verification subagents. Mostly citation-attribution fixes (a claim cited to a source that doesn't actually support it), not substantive claim errors: a fabricated Dowling-1978 URL replaced with the real DOI, three more mismatched auditory-perception citations corrected, a wrong Schellenberg-effect citation and a missing Nettl citation added on repetition-and-familiarity, a mischaracterized chill-trigger citation fixed and the anhedonia prevalence figure softened on pleasure-and-reward, Berlyne's collative-variables/two-system model re-attributed to its actual source on complexity-and-preference, and two citation/attribution fixes plus a softened claim on musical-expectation. Details: `logs/sessions/004-2026-07-06.md`.
- **Algorithms pages (`reviewed`, session 005):** algorithmic-composition-history, markov-and-statistical-models, grammars-and-rewriting-systems, constraint-and-rule-based-methods, stochastic-chaos-and-automata, machine-learning-music — verified via six parallel verification subagents. The domain was in strong shape (its two most specific empirical claims — Nelias & Geisel's 4–100-quarter-note correlation cutoff and Levitin's Beethoven>Mozart rhythm predictability — both confirmed with correct direction). Fixes were mostly citation/URL integrity: a wrong-target AAAI URL on markov (`…/9130`, an unrelated face-recognition paper → `…/9126`) plus an IDyOM link swap; an off-by-one L-systems DOI and a muddled Steedman secondary citation replaced with the primary on grammars, plus a Lidov & Gabura "rhythm"→melody correction; an unverifiable Hofstadter quote replaced with his documented *Virtual Music* wording and a Lewis open-access mirror added on history; "~2,000"→1,788 movements and a URL-slug fix on stochastic; an over-gloss and a MorpheuS DOI/year tightening on constraint; and — the biggest single fix — machine-learning-music's flagged lint debt resolved by supplying verified canonical URLs for all eight of its previously URL-less sources. Details: `logs/sessions/005-2026-07-06.md`.
- **New page (session 003):** `indian-popular-and-film-music.md` (filmi/Bollywood + regional pop; `draft`) — closes the Indian popular/film coverage gap ahead of the launch engine; frames the engine as classical raga/drone core + mukhda/antara sectioning + Western rhythm + an optional raga-constrained chord filter. Still awaiting its own light verification pass (queued below).
- Process files live: `CLAUDE.md`, `continue-prompt.md`, `README.md`, this file, `logs/sessions/`.
- **All 8 open questions answered (2026-07-06) and propagated** across the wiki + process docs (session 002); **two revised by Tom on 2026-07-07** (session 009). Operative decisions now live in `wiki/project-open-questions.md` → "Answered" and are summarized in `CLAUDE.md` → "Session budget, tools, and standing decisions." Highlights: dependency-free at runtime by *default*, with **original first-party libraries preferred over third-party imports** when shared code is needed (revised 2026-07-07; plan in `shared-libraries.md`); **everything public-domain CC0 — code *and* prose** (single `LICENSE`; revised 2026-07-07); feedback via on-page form → downloadable JSON; launch shortlist swaps gamelan for an **Indian classical + pop** engine; daily site (Phase 4) refreshed when >24 h since last update; evaluator pool is Tom-only for now; sources in any language, wiki in English; ~70% context budget + OpenRouter (≤$5/UTC-day).

## Task queue (next sessions, roughly in order)

**Phase 3 (the comprehensive site) is now the primary track** per Tom's 2026-07-09 directive; Phase 2 engine work continues where it feeds the site. The 2026-07-08 steer (composition depth + more engines, de-prioritize Engine-02 static) is superseded where it conflicts. Highest-value next steps:

- **P3-1. Verification passes over the 26 new session-028 draft pages** (the established one-agent-per-page pattern; every page's completion report lists its own 2 riskiest claims — start there). Prioritize the design pillar (style-invention, hybridization, meta-composition, control-surfaces, music-representation) since design docs will build on them; then theory/craft; then the genre surveys. Also wire **back-links from the old hub pages to the new pages** (harmony → modal-and-nonfunctional-harmony; timbre-and-orchestration → texture + instrument-acoustics; melody → thematic-development + ornamentation; form-and-structure → beginnings-endings; style-and-genre-overview → style-invention + hybridization; groove → funk + hip-hop; west-african-rhythm → sub-saharan; gamelan/east-asian → southeast-asian; algorithmic-composition-history → meta-composition; engine-architecture → music-representation) — deferred from session 028 for budget; lint is green without them but discoverability wants them.
- **P3-2. Phase 3b design docs** — **prepared in `docs/` for Tom's async review** (his 2026-07-09 instruction; durable decisions also filed in the wiki), per comprehensive-site-vision: (a) the **control taxonomy — ✅ DELIVERED session 030, REVISED to rev. 2 session 031** (`docs/design/control-taxonomy.html`): the three-tier control lists + the 8-genre Start shortlist + a live-setting-change design (§8); **Tom's first-round decisions folded in** (multi-select genre melds max 2, broadened Folk, three modes with a top selector, live changes confirmed feasible). Small follow-ups remain (two-genre meld default balance + which parent owns which axis; the live genre-swap transition), but the tier design is settled; (b) the **style-pack / style-vector schema — ✅ DELIVERED session 032** (`docs/design/style-vector-schema.html`): the fielded style vector (~24 fields, 4 layers, typed cat/ord/cont), the two-genre meld (per-axis parent selection + even balance + compatibility gates), the coherence gate, the live-morph data flow (2-bar crossfade), a serialization sketch (destination-snapshot only), and the three shared generators; (c) the **site & library architecture — ✅ DELIVERED session 033** (`docs/design/site-architecture.html`): folder layout, module list, the just-in-time composition loop, live-change mechanics, and a walking-skeleton build order. Order: taxonomy ✅ → schema ✅ → architecture ✅ — **Phase 3b design docs COMPLETE.** When Tom signs off, fold the settled taxonomy + schema + architecture decisions into the wiki (`control-surfaces-and-user-parameters.md` / `engine-architecture.md` + likely a new style-schema page).
- **P3c. Build the comprehensive site — ✅ DONE (session 034, M1–M8 all landed; see "Where things stand").** What follows it, in order:
  - **P3d-0. Tom's listening loop (ongoing).** **NEW (session 039): listen for the expression layer and the new voices** — does the classical lead sing (legato/rubato/scoop); do `organ`/`horn`/`voce` read as rich and alive (is voce pleasing or uncanny); are invented pieces varied with no residual repeated-note cases; should electronic/folk bind more or less legato. Carried from 038: do the 8 invented architectures read as different engines; gamut audibility (now actually in-scale after the 039 fix); texture weights; percussion quietness in chorale/tintinnabuli; the rondo extension on long pieces. The on-page feedback JSON captures seed+settings exactly; the structure window names each invented piece's architecture.
  - **P3d-1. Preset tuning pass** per genre from his notes (centers/spreads are deliberately first-pass; the improvement loop owns them now), plus meld balance/per-pair tuning (even 50/50 is provisional by Tom's own flag).
  - **P3d-2. Verify `findings-comprehensive-site.md`** (new draft) in the standard pass, and **graduate the settled design-doc decisions into the wiki** (control taxonomy → `control-surfaces-and-user-parameters.md`; schema + JIT/live-change architecture → `engine-architecture.md` or a new page) once Tom confirms the built reality matches his intent.
  - **P3d-3. Deferred build items:** a vinyl-crackle texture voice for the shared synth (lofi wants it); Advanced per-layer overrides (taxonomy §6.2) beyond the current board; stress rapid repeated genre swaps; an automation-capture option remains explicitly dropped (Tom). (`cadenceDrop` was realized in session 038 as the invented ending's melodic fall.)
  - **P3d-4. Jazz rework (from Tom's 2026-07-11 reaction — withdrawn from the UI in session 039).** What it needs to re-earn its Start button (engine-depth work, not preset tuning): a real ride-cymbal pattern with swing interpretation, walking bass with voice-leading targets, idiomatic comping (shell/rootless voicings, anticipations), blues inflection in the melodic vocabulary; audition against `wiki/jazz-and-improvisation.md`. Un-hide by removing `jazz` from `HIDDEN_STYLES` in `docs/app.js` — everything else still works (the pack stayed registered).
  - **P3d-5. Full offline render sweep** (`render-site.mjs`, all genres + melds at full length) whenever a session lands in a faster environment — carried from 036–039; the bounded gates (render-invent-quick + per-change one-offs) have covered the interim.
- **P3-3. Small reconciliation:** update `engine-architecture.md`'s documented schemas to shipped practice (`freq` not `freqHz`; document `beat`/`midi`/`tags`/`weight?`/`expr?` pass-through) — found by the music-representation agent reading all five engines. **Also fold the live-setting-change model (session 031, §8 of the control taxonomy) into `engine-architecture.md`** — the site's just-in-time composition + three response speeds are a new architectural requirement the current page (built around whole-piece-up-front engines) doesn't cover.
- **P3-4. Verify the session-031 Baltic addition** to `european-folk-and-dance.md` in the next verification pass (subagent-written, cited, hedged — the standard one-agent-per-topic check). The subagent explicitly flagged as unverified: skudučiai "one or two pitches per player" (inferential), the kanklės funerary-use / women-forbidden claims (only the *kokle* sacred associations are solidly sourced), absolute Baltic dance tempos (none found), and the "archaic"/age datings incl. Seto leelo "~2,000 years" — all hedged in-text.

- **A. Build the next engine(s) + deepen composition (Tom's priority now).** Five engines now cover five contrasting worlds: tonal-classical, ambient-drift, groove-lofi, **cantabile** (Engine 04 — expressive small ensemble, session 024), and **percussion** (Engine 05 — an ensemble across concert/folk/drum-circle worlds, session 026). Extend the catalog to more music types. **Reusable asset from Engine 05:** the shared `synth.js` now has **seven original percussion voices on three generators** (pitch-drop membrane, modal bank, shaped noise) + a `note.p` timbre-macro surface, and the percussion composer's **timeline + interlocking-ostinati + lead + density-arc** architecture and **meter-parametric** grid generalize to any rhythm-forward engine (see `wiki/percussion-music.md`, `wiki/percussion-sound-design.md`, `findings-percussion-engine.md`). **Reusable asset from Engine 04:** the shared `synth.js` now has a **continuous intra-note expression layer** (`note.expr` + `pitchExpr`/`exprEnv`/`breathLayer`/`panTo`) and four expressive voices — future engines that want singing lines can vendor and reuse it, and the per-note **structural-weight → expression-drive** interface generalizes. **v0.2 (session 025) added original-sound-design helpers** (`microDrift`/`bodyResonance`/`onsetTransient`) that make *any* sustained voice sound less like a generic synth (see `wiki/original-sound-design.md`), plus a reusable **voice-led + pattern-based accompaniment** approach and **breath-delimited vocal phrasing** in the composer/performer — all directly reusable by later engines. Shortlist/ideas already in the wiki: **Indian classical + pop** (raga/drone core + mukhda/antara sectioning + Western rhythm + optional raga-constrained chord filter — needs the non-cadential endings idiom; see `wiki/indian-popular-and-film-music.md`), and the other `wiki/project-roadmap.md` Phase-2 candidates. Each new engine is a self-contained numbered folder under `docs/engines/` that vendors the shared libs and is added to the hub. Composition depth: the carried increments below (voice-leading, tonicization, walking bass, real endings) plus new forms/harmonic vocabularies. Keep findings in the wiki.
- **A-static (DEFERRED per Tom). Engine-02 "static" is NOT resolved.** Sessions 021/022/023 fixed *three real, distinct* things (reverb-tail grain; the Engine-01 envelope puff → v0.3.1; the Engine-02 crossfade/bell-cluster density → v0.1.2), each offline-verified — **but Tom still hears static on Engine 02 v0.1.2 (`seed=drift-1`).** Working assessment (see the "Where things stand" Engine-02 bullet for the full reasoning): **the residual is most likely in the SHARED libraries — the *sound* of the `synth.js` `pad`/`bell` voices and/or the long `fx.js` reverb — exposed by ambient's quiet/sustained texture, NOT Engine-02-composer-specific** (the density fix cleaned the flagged windows in measurement but not to the ear → points at shared sound-generation, not arrangement). **Decisive test when we return:** audition a single isolated sustained note from each shared voice (dry + reverb); lone-note static ⇒ shared-library, stacking-only static ⇒ arrangement. **First rule out:** confirm Tom heard *true v0.1.2* (Pages cache — check the version badge). Also still open from session 021: do Engine 01's varied contours read as different-yet-pleasing, and does Engine 03's comp sit *under* the beat?
- **B. Engine 03 — groove/lo-fi — DONE (session 020).** `groove-lofi@0.1.0` live at `docs/engines/03-groove-lofi/`; completed the trio and built the percussion voices (`kick`/`snare`/`hat`) + swing/laid-back timing. **Next increments on it** (`findings-groove-lofi-engine.md` "open questions"): automate the syncopation target (wire the polyphonic Witek/LHL index into a kick-pattern selector — a step toward R4); a real ending/turnaround vs. the current thinned outro; inter-chord voice-leading + a walking-bass option; a table of per-genre microtiming feels (the performer is already built to carry them). The other shortlist option, **Indian classical + pop**, still needs the non-cadential endings idiom.
- **B2. Carried-over Engine 01 increments** (still open after v0.2): **inter-chord voice-leading** (chords are close-position triads + octave bass), the **minor-key cross-relation** fix (natural-minor melody vs harmonic-minor V), and a brief **B-section tonicization**. All small, testable increments on `composePiece()`.
- **C. GitHub Pages — DONE (enabled by Tom, ~2026-07-07).** The hub + all engines are live online and **Tom listens through the public site** (confirmed 2026-07-09). No further action; do not remind him again.
- **D. Feedback-loop mechanics.** The engine writes a feedback JSON; specify how those accumulate and get handed back (the schema/aggregation detail flagged in `engine-architecture.md` open questions + `listening-tests-and-feedback.md`).

**Continuing Phase-1 knowledge work (still valuable, lower priority now):**

1. **Verify-and-mature pass — DONE (session 015).** All 60 content pages plus the 1 findings page are now `reviewed` (63/63); the first full pass over the entire wiki closed with `improvement-loop.md`, `shared-libraries.md`, and `indian-popular-and-film-music.md`. Pattern that worked well thirteen times running, worth reusing for any future new/re-opened draft: one verification subagent per page, main session owns the edits, cross-links, and index; verify subagent claims before merging (quotes in quotation marks are the highest-risk claim type; insist subagents fetch primary sources rather than trust WebFetch's HTML summarization or a press release's paraphrase; when a page cross-references sibling wiki pages, check the *current* state of those pages, not what they said when the page under review was written — this caught real drift in sessions 014 and 015). Going forward, verification is no longer a standing queue item but something to re-run only if a page is edited substantively or a new page is drafted.
2. **Cross-domain lint — first full pass done (session 013), but not exhaustive; worth a second pass now that all 63 pages are `reviewed`.** Four parallel subagents (theory+craft, psychology, genres, algorithms+implementation+evaluation clusters) checked internal consistency and the three flagged shared-topic categories (tempo/BPM, consonance/dissonance, rhythm-category/small-integer-ratio); found and fixed 4 real contradictions (octave equivalence, a 3-way swing-ratio mismatch, a click-avoidance-ramp conflict, a voice-count-cap ambiguity — see `wiki/log.md` and `logs/sessions/013-2026-07-07.md` for details) and ruled out several suspected duplications as consistent-not-contradictory. This pass leaned on the three pre-flagged topics; it did **not** exhaustively cross-check every other recurring number/claim (named-study citation consistency beyond the flagged topics, register/range numbers, dynamics/dB figures were not systematically cross-compared), and it predates the 9 pages promoted in sessions 014–015 (the three doctrine syntheses + improvement-loop + shared-libraries + indian-popular-and-film-music), none of which have been cross-checked against the rest of the reviewed set for contradictions yet — good next-session scope. The mechanical checks remain automated — run `node experiments/tools/wiki-lint.mjs` every session (links, frontmatter, sections, orphans; `--index` dumps frontmatter for index maintenance) — it reported 0 problems across 63 pages as of session 015.
3. **Source notes** for cornerstone sources in `wiki/sources/` (Huron *Sweet Anticipation*; Meyer 1956; Margulis *On Repeat*; Gjerdingen schemata; KTH rule-system papers; Lerdahl tension; Bregman ASA; Toussaint; Fux; Eno's generative talks; and — for the Indian engine — Beaster-Jones *Bollywood Sounds* and Arnold 1988) — 2–3 per session, deep and citable. Note (session 015): Beaster-Jones's *Bollywood Sounds* and Booth's *Behind the Curtain* both resisted automated fetch (Oxford Academic and Google Books returned no usable full text) — a source-note session for these may need a different access route (library proxy, ISBN-based excerpt search, or accept a thinner note than usual).
4. **Research questions R1–R8** in `wiki/project-open-questions.md` — each is a session-sized design study whose output is a wiki page. **R1 (phrase-first melody) and R2 (goal-directed harmony) are now WORKED (session 017)** — assembled into `experiments/composers/tonal-phrase.js` and documented in `wiki/findings-tonal-phrase-composer.md`; they no longer gate Phase 2. Highest-value remaining: **R6 (endings** — engines must be able to *finish* a piece, which no previous experiment could), **R4 (cadence detector + motif-recurrence metric** — gates the self-evaluation loop; the `analysis` module and harness are now a foundation for it), and R3's remaining piece (validate the expressive-performance magnitudes on synthesized timbres, now that the render harness exists).
5. **Declared coverage gaps**: Latin American traditions; European folk/dance musics; Arabic maqam page; glossary; notation/representation. (Indian popular/film music done — session 003, verified session 015.) (Grow this list as reading reveals more.)
6. **Corpus groundwork**: license-vetted corpus shortlist + first extraction script + first reference table (interval/phrase-length distributions for one style).
7. **Optional testbeds** — **OfflineAudioContext render-and-measure harness DONE (session 017):** `experiments/tools/render-measure.mjs` + `experiments/lib/analysis.js` + `experiments/demos/offline-render.html` validated `transport`'s sample-accurate timing and click-safety, and render the composed period; run `node experiments/tools/render-measure.mjs`. Remaining testbed ideas: extend `analysis` with spectral flux / chroma / a BS.1770-style loudness meter (the wiki's fuller `computational-music-metrics.md` set); scheduling/synthesis recipe smoke tests once `synth` exists.
8. **Shared-library research & prototyping** (Tom-requested 2026-07-07). **Foundation trio complete + audio-timing confirmed + analysis core prototyped (sessions 010 + 016 + 017):** `rng`, `transport`, `theory`, and the `analysis` measurement core all in `experiments/lib/` and headless-tested (74 tests); the OfflineAudioContext harness (session 017) validated `transport`'s **sample-accurate real audio timing**, closing the last gap before an engine can depend on the foundation. The dual-format (UMD-lite, `file://`-safe) vendoring format and the "oracle practice" question are settled (findings in `wiki/findings-shared-lib-foundation.md`). **Remaining:** the **audible layer — `synth` and `fx`** (build-order item 2, now unblocked and validatable with the harness), then the rest of `analysis` (spectral/chroma features + symbolic score metrics). Original code only — reimplement published algorithms, never copy source. The R1/R2 composition design that was the flagged next scope is now DONE (`experiments/composers/tonal-phrase.js`, `wiki/findings-tonal-phrase-composer.md`).

## Standing reminders

- Session end = status updated + session log + wiki log/index current + committed + pushed + PR + **squash-merged** + merge confirmed.
- Check `wiki/project-open-questions.md` each session start for answers from Tom; propagate any into affected pages and move to "Answered." (All 12 recorded questions are answered — 8 bootstrap + 4 comprehensive-site; the section is ready for new ones.)
- **Review docs → `docs/`** (Tom, 2026-07-09): documents/pages prepared for Tom's review go in `docs/` (readable via GitHub Pages); temporary ones may be deleted once superseded; durable decisions still land in the wiki. The five preliminary test engines live in `docs/preliminary-tests/`; the site entry point is `docs/index.html`.
- **Budget/tools** (Tom, 2026-07-06): aim for one theme done well within ~70% of the context window; OpenRouter is available (`OPENROUTER_API_KEY`) for second opinions / web searches up to $5 per UTC day — note nontrivial use in the session log.
- **Licensing** settled (revised 2026-07-07): everything — code *and* prose — is public-domain CC0 (single `LICENSE`; the separate `LICENSE-wiki` was removed) — don't paste in incompatibly-licensed text or code.
- **Dependency policy** (revised 2026-07-07): runtime dependency-free by default; when shared code is genuinely needed, build the project's *own original first-party libraries* (vendored per engine, reusing no outside project's code) rather than importing third-party code — see `wiki/shared-libraries.md`. No absolute ban on external libraries, but original code is strongly preferred.
- **Daily-site UTC check** applies only in Phase 4 (once `docs/` summary site exists): if >24 h since last site update, refresh at session end.
- GitHub Pages is **enabled** (deploying from `main`/`docs/`); the hub + engines are live online and **Tom listens through the public site** — no need to remind him to enable it (settled 2026-07-09).

## Session history

| # | Date | Model (as reported) | Summary |
|---|---|---|---|
| 039 | 2026-07-11 | Claude (Fable) | **Tom's third feedback pass: expression layer + rich voices + invented repeated-note fix + Jazz withdrawn.** The JIT loop stays (form is known at `init()`): `perform.js` gains a phrase-analyzed expression layer (legato overlap, portamento, deeper structure-aware rubato + agogics, per-phrase dynamics, arc-driven intra-note expression, all live-tunable); three new original sustained voices (`organ`/`horn`/`voce`) wired into palettes/ensembles/checkboxes (V3 URL, 27-bit mask); Tom's mostly-one-note invented piece traced to the 038 gamut offsets-as-steps bug + chorale-over-drone chord-locking + strata parking — all fixed with standing monotony gates; Jazz hidden from the picker (pack stays registered; reaction + revisit note in the wiki). 192 node tests; all sweeps clean; smoke 45/45. Details: `logs/sessions/039-2026-07-11.md`. |
| 034–038 | 2026-07-09 → 07-10 | Claude (034/038 Fable per hand-off prompts; 035–037 unrecorded) | Rows unmaintained at the time — see "Where things stand" bullets + `logs/sessions/034…038-*.md`: 034 built the comprehensive site (M1–M8); 035 first-listen UI pass (single-select, transport-first, continuous play); 036 second feedback pass (palettes, instrument checkboxes, new Advanced params, structure window, V2 URL); 037 invented ensembles 6→32 + names; 038 the dedicated invented-style composer (per-seed kernel, `docs/lib/invent.js`). |
| 033 | 2026-07-09 | Claude (Opus 4.8) | **Third/final Phase-3b design doc — the site & library architecture — + a Fable build prompt.** Wrote `docs/design/site-architecture.html`: the one-folder layout (site = `docs/index.html` + `lib/` + `styles/` + `app.js`; classic scripts, `file://`-runnable), the shared-library module list (existing `rng/transport/theory/synth/fx/analysis` + new `generators/style/compose/perform/serialize`), the runtime pipeline and the **just-in-time composition loop** (compose a bar/phrase ahead of the playhead reading the live style vector — the no-restart live-change mechanism), the three response speeds + 2-bar crossfade, determinism/seeding, compact URL, how the 5 engines generalize, and a **walking-skeleton build order** (M1 skeleton → M2 one genre → M3 3-mode UI+URL → M4 all 8 → M5 melds → M6 live → M7 invented → M8 polish; M1–M4 = one-session floor). Completes the three Phase-3b design docs. Then wrote a **self-contained build prompt for Fable** (preserved in this session log) to do the Phase-3c build in one session. All four design pages render clean headless (0 errors). Details: `logs/sessions/033-2026-07-09.md`. |
| 032 | 2026-07-09 | Claude (Opus 4.8) | **Second Phase-3b design doc — the style-vector schema.** On Tom's three follow-up answers (meld balance **even**/provisional; live genre-swap = **two-bar crossfade**/provisional; shared link = **destination snapshot only**), wrote `docs/design/style-vector-schema.html`: the **style vector** as ~24 typed fields in 4 layers (material/per-voice/evolution/surface); presets vs invented styles (novelty budget + authored signature rules) vs melds; the **two-genre meld** in full (per-axis parent selection not averaging — categorical snap / continuous blend, rhythm-chassis ≠ harmony parent, even balance, compatibility gates, cap 2, worked examples, per `hybridization-and-fusion.md`); the coherence gate; the determinism + live-morph flow; a destination-snapshot serialization sketch; and three shared generators (Schillinger/Xenakis/Koenig). Reconciled the control taxonomy to match (even balance, 2-bar crossfade, snapshot-only, cross-links). All three design pages render clean headless (0 errors, 8 genre rows). Details: `logs/sessions/032-2026-07-09.md`. |
| 031 | 2026-07-09 | Claude (Opus 4.8) | **Tom's control-taxonomy decisions + live-change design (rev. 2).** Folded Tom's first-round review calls into `docs/design/control-taxonomy.html`: **Genre is now multi-select (max 2, two blend into a meld** — a two-preset hybrid per `hybridization-and-fusion.md`, not on-the-fly invention); **Folk broadened** Celtic-only → pan-European; **three distinct modes with a top selector** (settings preserved); Energy adapts per genre + combination; Intermediate + invented params accepted. Added **§8 live setting changes** — confirmed feasible via just-in-time composition (compose a bar/phrase ahead of the playhead, read current control values), three response speeds (instant / next-boundary / re-plan), and URL = current-settings snapshot (destination reproducible, exact live morph not; optional automation-capture deferred). Also **added a Baltic section** to `european-folk-and-dance.md` (sutartinės canon-in-seconds, drone box-zithers, hocket pan-pipes; via a research subagent — cited, hedged, `draft`) backing the broadened Folk genre; `wiki-lint` 0/98. Design pages render clean headless (0 errors, 8 genre rows). Details: `logs/sessions/031-2026-07-09.md`. |
| 030 | 2026-07-09 | Claude (Opus 4.8) | **First Phase-3b design doc — the control taxonomy.** On Tom's request, wrote `docs/design/control-taxonomy.html` (+ a `docs/design/index.html` review-docs index): the three-tier control lists (Start ~5 + Play/dice; Intermediate ~20 familiar, grouped, each with type/range/default/mapping; Advanced = the 13 style-vector axes + per-layer overrides + 8 invented parameters), seven design principles (auto=seed-decides, deterministic inputs, progressive disclosure, perceptual labels/scaling, defaults-are-the-product, constrained randomize, failure-mode guards), and a proposed **8-genre Start shortlist** (Classical · Ambient · Lo-fi beats · Jazz · Folk/Celtic · Electronic/House · Cinematic · Percussion) grounded by a research subagent against the wiki genre pages (tempos/character cited; 5 map to existing engines, 3 new builds; Blues/Minimalism as swap-ins). Built on `control-surfaces-and-user-parameters.md` + `style-invention-and-style-space.md`; headless render clean (0 console errors, 8 genre rows). Awaiting Tom's review (7 questions in §9, Start shortlist first). Details: `logs/sessions/030-2026-07-09.md`. |
| 029 | 2026-07-09 | Claude (Opus 4.8) | **Tom's 2026-07-09 answers propagated + `docs/` reorganized for the comprehensive site.** Answered the four comprehensive-site questions (engines stay live → moved to `docs/preliminary-tests/`; the new site's `index.html` goes directly under `docs/` as the public entry point; ~6–10 Start genres with Claude proposing the shortlist; design-review cadence stands, and **review docs now go in `docs/`**). Relocated the hub + engines intact via `git mv` (engines → `docs/preliminary-tests/engines/`, hub → `docs/preliminary-tests/index.html`; every internal link preserved, engine folders byte-frozen); wrote a new `docs/index.html` landing page (vision + three control tiers, Claude-authorship/CC0 transparency, links to the preliminary tests); updated the 6 render/ui-smoke harnesses, the 5 findings pages, `music-representation-and-notation.md`, and all process docs to the new paths; propagated the answers into `comprehensive-site-vision.md`, `project-roadmap.md`, `project-open-questions.md`. 141 tests; wiki-lint 0/98; ui-smoke 19/19; render-engine 16/16; render-percussion 43/43. Details: `logs/sessions/029-2026-07-09.md`. |
| 028 | 2026-07-09 | Fable 5 (orchestrator) + Sonnet 5 / Opus 4.8 subagents | **Tom defined the long-term goal + 26-page wide-coverage wiki expansion.** The goal: one self-contained site composing many pre-established AND on-the-fly original styles, three control tiers, compact seeded URLs, original annotated shared libraries, one folder — recorded verbatim in the session log, distilled in new `wiki/comprehensive-site-vision.md`; roadmap Phase 3 redrawn (3a wiki expansion → 3b design docs → 3c build → 3d loop); 4 working-assumption questions filed for Tom. Then, per Tom's instruction (full session on the wiki; Fable 5 orchestrating Sonnet 5/Opus 4.8 subagents), **25 research agents wrote 25 sourced draft pages** + 1 orchestrator page: design pillar (style-invention, hybridization, meta-composition, control-surfaces, music-representation), theory/craft (texture, modal harmony, ornamentation, thematic development, beginnings/endings, tempo/duration, instrument acoustics), psychology (memory/earworms), world traditions (Latin American, reggae/Caribbean, Arabic/Middle Eastern, sub-Saharan, European folk, Southeast Asian), popular/art genres (rock/pop, blues/country, funk/soul, hip-hop, contemporary art music, medieval/Renaissance). Wiki 72 → **98 pages**; `wiki-lint` **0/98**; index gained a site-design group + reading path; four long-declared gaps closed. All new pages `draft` → verification queued (P3-1), then design docs (P3-2). Details: `logs/sessions/028-2026-07-09.md`. |
| 027 | 2026-07-09 | Claude (Opus 4.8) | **Engine 05 → v0.2.0 — sliding scales + 4 more voices.** On Tom's ask (more variety in genres/development as *sliding scales*, and 4 more instruments varied by seed), replaced the three fixed styles with a single **parametric generator** driven by a **seed-sampled recipe** (axes: development, form type, an intensity-arc from **9** shapes, timeline type incl. Xenakis **sieve**, density, looseness, lead, call-response, interlock, pitched). The three genres are now presets; **`auto`** (default) roams the whole space; every axis is user-overridable (each UI control defaults to "auto = seed decides"). Added a **seed-varied ensemble sampler** (instrument selection/combination varies by seed; user Ensemble presets) and **4 new original voices** of different types — `clap` (impact), `scrape` (rasp), `chime` (pitched metal), `friction` (cuíca bend) → **11 voices**. Over 24 auto seeds: 8 arcs, 5 timelines, 6 form types, 24 distinct ensembles. Additive/engine-05-local → engines 01–04 byte-for-byte unaffected. 141 tests; render-percussion 43/43 (8 cases, all 4 ensemble presets, palette-coverage gate = 12 voices); ui-smoke 17/17; wiki-lint 0/72. Also recorded that **GitHub Pages is enabled** (Tom listens via the public site; stop reminding). Details: `logs/sessions/027-2026-07-09.md`. |
| 026 | 2026-07-09 | Claude (Opus 4.8) | **Engine 05 (percussion) — an ensemble across three worlds.** Built `percussion@0.1.0` (`docs/engines/05-percussion/`) on Tom's brief: concert / folk / drum-circle percussion; original voices with adjustable timbre/attack/fade; default 2/4 or 4/4 in 4/8/16-bar groups, other meters selectable; each piece a form; melody only as accompaniment. **Wiki first:** two new pages — `percussion-music.md` (structure across the three worlds; density-arc form without harmony; timeline-vs-lead polarity; meter; generators) and `percussion-sound-design.md` (original synthesis: pitch-drop membranes with inharmonic 2-D modes, modal banks, shaped-noise gongs/shakers; six adjustable macros). **Engine:** seven original voices on three generators (membrane/modal/noise) reading a `note.p` params object; a three-style meter-parametric composer (concert cell-development → gong ending; folk timeline + Euclidean interlock + improvising lead; drum-circle heartbeat + rumble + stop-cut + fade) with nine meters and 4/8/16 phrase snapping; a performer with structured feel, per-section accel/rit, and four timbre macros; an optional subordinate mallet ostinato. Additive-only synth/fx (7 voices + 2 helpers + 4 buses), re-vendored only into engine 05 → engines 01–04 byte-for-byte unaffected. 138 tests (+15); render-percussion 31/31 (3 styles × 5 meters); ui-smoke 17/17; wiki-lint 0/72 (+3). New `findings-percussion-engine.md`; hub updated. Details: `logs/sessions/026-2026-07-09.md`. |
| 025 | 2026-07-09 | Claude (Opus 4.8) | **Engine 04 → v0.2.0 — Tom's three-note feedback pass.** (1) **Varied accompaniment** (composer): replaced the v0.1 equal-length ascending arp + bare root-on-1 bass with per-seed comping templates (broken/bass-chord/sync-stab/arp/pad, never repeated two bars running), inter-chord voice leading, and a varied bass (root/fifth/octave/walking/anticipation) — kept low (comp vel ≈ 0.32 vs leads ≈ 0.65) so it never covers the leads. (2) **Organic leads** (additive `synth.js` helpers on aria/reed/wire/glass): `microDrift` (always-on few-cent pitch wander), `bodyResonance` (fixed per-voice formant body), `onsetTransient` (per-voice attack noise) — the three highest-leverage cures for the "sounds like a synth" tells. (3) **Vocal phrasing** (composer + performer): breath-delimited motif-seeded sub-phrases; the performer carves a real silence at each breath, blocks portamento across it, hesitates entering a phrase, eases dynamics into it. Additive/engine-04-local → engines 01–03 byte-for-byte unaffected (regression 16/16 ×3). 123 tests (+5); render-cantabile 25/25 (peak ≤ 0.60, maxStep ≤ 0.064, cleaner than v0.1); ui-smoke 14/14; wiki-lint 0/69. New `wiki/original-sound-design.md`; `findings-cantabile-engine.md` extended. Details: `logs/sessions/025-2026-07-09.md`. |
| 024 | 2026-07-08 | Claude (Opus 4.8) | **Engine 04 (cantabile) — the expressive engine.** Built `cantabile@0.1.0` (`docs/engines/04-cantabile/`) on Tom's brief: music inspired by small groups with great per-note expressivity (string quartet / jazz sax / rock lead). Four **original** expressive voices (`aria`/`reed`/`wire`/`glass`, imitating no real instrument) get **continuous intra-note automation** — scoop/portamento, delayed blooming vibrato, *messa di voce* swell, blooming grit, release-bend — via new `synth.js` helpers (`pitchExpr`/`exprEnv`/`breathLayer`/`panTo`; `note.expr` schema). **Expression is tied to STRUCTURE:** the new `expressive-chamber` composer builds a modal dramatic arc (intro·theme·dialogue·rise·climax·return·coda) and stamps each note a structural **weight**; the performer scales every gesture by `Expression × weight × Ardor·intensity`, so the climax sings ~3–4× harder than the intro. **Original sliders:** Expression / Song / Bloom / Ardor / Rubato / Grain. Ensemble varies by seed + user pick (all four lead on some seed). Additive-only synth change; `fx.js` + engines 01–03 untouched. 118 tests (+13); `render-cantabile` 25/25; `ui-smoke` 14/14; regression render gates 16/16 ×3; wiki-lint 0/68. New `findings-cantabile-engine.md`; hub updated. Details: `logs/sessions/024-2026-07-08.md`. |
| 023 | 2026-07-08 | Claude (Opus 4.8) | **Engine 02 static — texture DENSITY, not the synth.** Tom still heard "static" on Engine 02 (`seed=drift-1`) after v0.1.1. A deterministic **OfflineAudioContext** render reproduces the signal — **peak/RMS matched Tom's 24-bit/48 kHz WAV to 3–4 sig figs**. Diagnosis (per-voice/reverb isolation, an amplitude-envelope **modulation-spectrum roughness** metric, active-note analysis) **ruled out** reverb (dry ≈ wet), clicks/envelope corners (`maxStep` 0.10), and per-voice synth tweaks. The static is **sensory roughness** (partials beating in a critical band; Plomp & Levelt) at **two structural moments**: region **crossfades** (the composer held two full pad voicings for an 8 s overlap) and dense **bell clusters** (up to 5 long inharmonic bells). Fixed in the **composer** (ambient-only): pad crossfade overlap **8 s → 4 s** (drone stays 8 s for continuous bass) + a **bell polyphony cap ≤ 4** + a mild ring trim. Both flagged windows drop to the clean level (−6/−4 dB); spectrograms confirm. `synth.js`/`fx.js` unchanged → tonal/groove untouched. **ambient-drift → 0.1.2.** 105 tests, render-ambient 16/16, ui-smoke 11/11, wiki-lint 0/67. Details: `logs/sessions/023-2026-07-08.md`. |
| 022 | 2026-07-08 | Claude (Opus 4.8) | **The real static fix — the synth envelope, not the reverb.** Tom's exact repro (extended phrase-final note, same in Chrome/Safari/Firefox, `seed=d8d8d8`) proved the static is deterministic and in the signal. Bisected offline to a **broadband "puff" at the envelope release onset**: the shared `synth.js` `envGain` held the gain **flat** at the peak then switched abruptly to a fast release — a slope discontinuity that splatters a broadband burst, mid-note on long notes. Fixed by never holding flat (struck voices decay continuously; sustained voices glide), exponential ramps only (identical in every browser). Spectrogram confirms the puff is gone. Versions → tonal 0.3.1 / groove 0.2.1 / ambient 0.1.1. 105 tests, render gates 16/16 ×3, ui-smoke 11/11, wiki-lint 0/67. Details: `logs/sessions/022-2026-07-08.md`. |
| 021 | 2026-07-08 | Claude (Opus 4.8) | **Second feedback pass on all three engines.** Diagnosed the recurring "static in the middle of sustained notes" (Engines 01 + 02) with headless OfflineAudioContext probes — renders are clean apart from **reverb-tail grain**; fixed the shared `fx.js` IR to a **near-Gaussian** noise source (grain cv 0.49→0.33, −31%, brightness/loudness unchanged; a control probe showed a 3rd pole *worsens* it, so it's not re-darkening), and added **`latencyHint: 'playback'`** to all three live contexts for real-time underruns. **Engine 01 → v0.3.0:** varied **melodic contour archetype per piece** (arch/descending/ascending/wave + contrasting B) so seeds no longer share one ascending-arpeggio arch. **Engine 03 → v0.2.0:** **voice-led** comp (centroid moves ≈1.3 semitones, a stable bed), per-seed **progression + comping-rhythm** banks, and a soft **sustained pad drone** — fixing "same every seed / overpowers the drone." New `ui-smoke.mjs` gate. 105 tests, render gates 16/16 ×3, ui-smoke 11/11, wiki-lint 0/67. Details: `logs/sessions/021-2026-07-08.md`. |
| 020 | 2026-07-08 | Claude (Opus 4.8) | **Engine 03 (groove-lofi) + compact collapsible hub.** Shipped `groove-lofi@0.1.0` (`docs/engines/03-groove-lofi/`), the trio's groove member and the project's first engine with drums: a looped lo-fi beat realizing `groove-and-embodiment.md` (backbeat on 2 & 4, medium-syncopation kick keeping beat 1, ghost notes, velocity hierarchy, 7th/9th Rhodes chords, weighty bass, vinyl bed). Swing (per-beat phase warp) and the laid-back backbeat (fixed ~18 ms offset) are performer-side structured timing over a quantized grid — not uniform jitter. Added `kick`/`snare`/`hat`/`rhodes` voices + drum buses to the shared `synth.js`/`fx.js`, re-vendored into all engines. Rebuilt the hub: engines 03→02→01, each a one-line head with an "About" `<details>` collapsed by default, more compact. 105 tests pass (+10); render gates 16/16 ×3; UI smoke 0 errors; wiki-lint 0 across 67 pages. New `findings-groove-lofi-engine.md`. Details: `logs/sessions/020-2026-07-08.md`. |
| 019 | 2026-07-08 | Claude (Opus 4.8) | **Engine 01 → v0.2 (feedback pass) + Engine 02.** Fixed tonal-classical v0.1's reported "static on the final note" (bright/noisy reverb wash + glue pumping, not clipping) with a darker/smoother IR, band-limited reverb return, gentler glue, and true-zero envelope tails; replaced the mechanical per-note white jitter with a real expressive layer (phrase-arch tempo+dynamics, high-loud, articulation, chord roll, vibrato, correlated AR(1) residual) per `expressive-performance.md`; raised default tempo 92→110. Shipped **Engine 02 — `ambient-drift@0.1.0`**: Eno's incommensurable-loop ambient recipe over a modal drift, four palettes, new shared `bell`/`pad`/`drone` voices + parameterizable dark reverb. 95 tests pass (+7); 16/16 + 16/16 render gates; new `findings-ambient-drift-engine.md`. Details: `logs/sessions/019-2026-07-08.md`. |
| 001 | 2026-07-06 | Fable 5 | Bootstrap: wiki schema + ~55 seed pages (11 research subagents + synthesis pages), process files, previous-experiments analysis. Details: `logs/sessions/001-2026-07-06.md`. |
| 002 | 2026-07-06 | Claude (Opus-class) | Answered all 8 open questions; propagated decisions across 15 files; added `LICENSE` (MIT) + `LICENSE-wiki` (CC0). Details: `logs/sessions/002-2026-07-06.md`. |
| 003 | 2026-07-06 | Claude (Opus-class) | Verified the theory core (5 pages → `reviewed`) and drafted `indian-popular-and-film-music.md`; fixed two real errors (tension-model weights, Axis count) + many citation upgrades. Details: `logs/sessions/003-2026-07-06.md`. |
| 004 | 2026-07-06 | Claude Sonnet 5 | Verified the psychology domain (6 pages → `reviewed`); found and fixed a fabricated citation URL (Dowling 1978) plus ~8 more source-misattribution errors across the six pages, no substantive claim errors. Details: `logs/sessions/004-2026-07-06.md`. |
| 005 | 2026-07-06 | Claude (Opus 4.8) | Verified the algorithms domain (6 pages → `reviewed`); fixed a wrong-target AAAI URL, an off-by-one DOI, a muddled/unverifiable-quote pair, and resolved machine-learning-music's URL-less-sources lint debt (8 canonical links added). Domain's specific empirical claims held up. Details: `logs/sessions/005-2026-07-06.md`. |
| 006 | 2026-07-06 | Claude Sonnet 5 | Verified the implementation domain (6 pages → `reviewed`) via MDN/W3C/GitHub/npm-registry fetches; fixed a wrong xylophone mode-ratio, an overstated float32-precision claim, a Freeverb citation misattributing STK's altered defaults, and an overstated Blob-URL-worklet browser-compat claim. Libraries survey's version/license/date facts all held up. Details: `logs/sessions/006-2026-07-06.md`. |
| 007 | 2026-07-07 | Claude Sonnet 5 | Verified the first 6 of 11 genres-domain pages (→ `reviewed`) via six parallel subagents fetching Wikipedia/arXiv/MTO/Dancecult/publisher sources; fixed a Frieler et al. corpus-size error, a Meyer publisher misattribution, an unsupported Brackett quote, a mis-cited Satie anecdote, a Koan misquote, a Solberg attribution overreach, and an unsupported downtempo BPM figure; hedged an unsupported Pärt-paper claim. 5 genre pages remain. Details: `logs/sessions/007-2026-07-07.md`. |
| 008 | 2026-07-07 | Claude Sonnet 5 | Verified the remaining 5 of 11 genres-domain pages (→ `reviewed`) via five parallel subagents, completing the genres domain (11/11). indian-classical-music held up entirely. Fixed a mis-cited RDR music claim (added the real composer-interview source), an invented ombak Hz example and an imprecise kotekan density figure, three quote-fidelity errors (Novotney/Anku/Agawu), an overstated guqin "documented practice" claim, a missing citation for the Chinese tonggong mode system, and an unsourced Sujecheon superlative. Details: `logs/sessions/008-2026-07-07.md`. |
| 009 | 2026-07-07 | Claude (Opus 4.8) | Propagated Tom's two 2026-07-07 policy revisions (dependency → prefer original first-party libraries; license → public-domain CC0 for code *and* prose, `LICENSE-wiki` folded into a single `LICENSE`); created `shared-libraries.md` (plan for the project's own libraries + the vendoring model); verified the craft domain (3 pages → `reviewed`), fixing KTH/Datseris/Prame numeric overstatements in expressive-performance, a Debussy letter date + Rimsky year in composition-craft, and a Hanslick-review misattribution (Grimes, not Wilfing) in what-makes-music-good. Details: `logs/sessions/009-2026-07-07.md`. |
| 010 | 2026-07-07 | Claude (Opus 4.8) | Verified the evaluation domain (4 pages → `reviewed`) via four parallel subagents — fixed a wrong Farbood slider rate (10→60 Hz) and an unsourced Essen corpus size (~10,000→sourced ~4,900 subset), split a Foote citation, tightened MUSHRA anchors; evaluation-challenges held up clean. Started the shared-library groundwork: prototyped + headless-tested `rng` and `transport` in `experiments/lib/` (25 assertions, exhaustive Euclidean maximal-evenness proof), filed `findings-shared-lib-foundation.md`, settled the dual-format `file://`-safe vendoring format. Reconciled a review-status drift in this file. Details: `logs/sessions/010-2026-07-07.md`. |
| 011 | 2026-07-07 | Claude Sonnet 5 | Verified the last 5 theory-domain pages (→ `reviewed`, completing the theory domain) via five parallel subagents fetching primary sources directly (PDF text extraction for Huron 2001, Witek et al. 2014, McAdams 1995/2019/2022, Fitch & Rosenfeld 2007). Fixed a wrong-author citation (Senn et al. 2022 → Stupacher, Wrede & Vuust) plus flagged a 2024/25 non-replication of the syncopation inverted-U; dropped an unsupported "brainstem" claim; corrected Moelants (2002)'s tempo-center figure; replaced "pulse reversal" with Fitch & Rosenfeld's actual term "resetting"; re-sourced a misattributed "reed organ" demonstration; softened a brightness→valence claim the cited sources actually disagree on; re-sourced a pseudo-polyphony citation. tuning-and-scales and counterpoint-and-voice-leading held up almost perfectly. Details: `logs/sessions/011-2026-07-07.md`. |
| 012 | 2026-07-07 | Claude Sonnet 5 | Verified the last 2 psychology-domain pages (→ `reviewed`, completing the psychology domain) via two parallel subagents fetching PMC/DOI/publisher sources directly. Fixed two citation errors (a wrong co-author, Perham & Currie 2011 → Perham & Vizard 2011; a fabricated author name, "Yorulmaz" → Awada et al. 2022), added a missing citation (Thompson, Schellenberg & Husain 2001), softened an unsupported irregular-AM-modulation claim, corrected a Mehr et al. (2019) figure (315→309 societies), and split a conflated Savage et al. (2015) claim (individual universals vs. co-occurrence network). Details: `logs/sessions/012-2026-07-07.md`. |
| 013 | 2026-07-07 | Claude Sonnet 5 | Ran the queued cross-domain consistency lint (first full pass) across all 54 `reviewed` pages via four parallel cluster subagents. Fixed 4 real contradictions: an octave-equivalence claim in auditory-perception-basics.md that contradicted the page it cited as support (musical-universals.md documents it as contested); a three-way swing-ratio-by-tempo mismatch across rhythm-and-meter.md, expressive-performance.md, and jazz-and-improvisation.md (the last also self-contradictory) — reconciled to one canonical statement in jazz-and-improvisation.md with a new verified drummer-vs-soloist nuance; a click-avoidance minimum-ramp conflict between web-audio-fundamentals.md and synthesis-recipes.md's percussion attacks; a minor ≤3 vs ≤3–4 voice-count-cap discrepancy, clarified via cross-reference. Ruled out several suspected duplications as consistent, not contradictory. Details: `logs/sessions/013-2026-07-07.md`. |
| 014 | 2026-07-07 | Claude Sonnet 5 | Verified the three doctrine syntheses (→ `reviewed`) via three parallel subagents: generative-music-failure-modes and generative-music-design-patterns held up with zero drift; engine-architecture.md had one real fix — its "Runtime rules" section claimed engines could use ES modules, contradicting the session-010 finding that cross-file `import` is CORS-blocked under `file://` — rewritten to state classic-script-only vendoring. 57 of 60 content pages now `reviewed`. Details: `logs/sessions/014-2026-07-07.md`. |
| 015 | 2026-07-07 | Claude Sonnet 5 | Verified the last 3 draft pages (→ `reviewed`) via three parallel subagents, completing the first full verify-and-mature pass over the entire wiki (63/63 pages now `reviewed`). improvement-loop.md: fixed a findings-page filename convention that neither conventions.md nor the actual findings page followed. shared-libraries.md: checked claim-by-claim against experiments/lib/rng.js and transport.js; fixed a "25 assertions"→"25 tests" imprecision (also fixed in findings-shared-lib-foundation.md) and tightened the vendoring-model section. indian-popular-and-film-music.md: light source-fetching pass — found a peer-reviewed source (Dias & Jähnichen 2014) for the general Western-chord-over-raga phenomenon, reconstructed the still-inaccessible scroll.in Rahman quote from search snippets, corrected an overstated Diljit Dosanjh "Coachella headliner" claim; the specific I–vi–IV–V and raga↔mode-mapping claims remain honestly hedged (Beaster-Jones/Booth still inaccessible to fetch). Details: `logs/sessions/015-2026-07-07.md`. |
| 016 | 2026-07-08 | Claude Sonnet 5 | Built `experiments/lib/theory.js`, the third and last shared-library foundation module (notes/MIDI/frequency, intervals, 13 scales, Krumhansl-Kessler weights, generic tertian chord construction, T/S/D functional labels, Piston/rock-corpus root-motion priors, cents-based tuning tables) — 26 new tests (51 total passing) plus a dev-time-only Tonal oracle cross-check (128 MIDI notes, 156 scale instances, 24 diatonic-key instances, zero mismatches; Tonal never added as a dependency). Updated `findings-shared-lib-foundation.md` and `shared-libraries.md` (prototype status, build order, resolved the "oracle practice" open question). Completes the foundation trio; only an OfflineAudioContext render-and-measure harness remains before an engine can depend on it. Details: `logs/sessions/016-2026-07-08.md`. |
| 018 | 2026-07-08 | Claude (Opus 4.8) | **Phase 2 launched.** Shipped Engine 01 — `tonal-classical@0.1.0` (`docs/engines/01-tonal-classical/`) + hub `docs/index.html`. Extended the composer with `composePiece()` (intro + rounded ternary A B A′ + coda: ornamented return = motivic variation, contrasting B, accelerated-cadence ending = R6). Built the audible layer: `synth.js` (FM keys/detuned strings/bass) + `fx.js` (buses/convolution reverb/glue+limiter). Performer + scheduler + UI (two-level controls, URL-hash share, piano-roll, JSON feedback). 88 tests pass (+14); 16/16 offline-render audio gates. New page `findings-tonal-classical-engine.md`; Phase-2 flip across all process docs. Details: `logs/sessions/018-2026-07-08.md`. |
| 017 | 2026-07-08 | Claude (Opus 4.8) | Completed the two Phase-2 preparations Tom asked for. (1) Worked R1/R2 composer `experiments/composers/tonal-phrase.js`: phrase-first melody over a goal-directed, cadence-first progression as an eight-bar parallel period, emitting the engine-architecture Note-event schema deterministically — harmony via a backward Piston walk (pre-dominant-before-dominant falls out for free), melody contour-first over fixed goal tones; 15 structural tests; new page `findings-tonal-phrase-composer.md`; R1/R2 marked worked. (2) OfflineAudioContext render-and-measure harness — `analysis.js` (pure PCM metrics, 8 tests) + `offline-render.html` + `render-measure.mjs` (Playwright) — validated `transport` timing as sample-accurate (jitter 0.015 ms, max err 0.24 ms), confirmed click-safety (0.018 vs 0.50), rendered the composed period clean; closed the audio-timing gap. 74 tests passing; harness 12/12 gates; wiki-lint 0 problems across 64 pages. Details: `logs/sessions/017-2026-07-08.md`. |
