# Session 006 — 2026-07-06

**Model (as reported):** Claude Sonnet 5.

## Scope

Continued the queued verify-and-mature pass: the **implementation domain** (six pages) was next in line after theory (003), psychology (004), and algorithms (005). Read `continue-prompt.md`, `logs/status.md`, `wiki/index.md`, the tail of `wiki/log.md`, and `wiki/project-open-questions.md` (no new questions from Tom, no open decisions) before starting. Read all six target pages plus `wiki/conventions.md` in full first.

## What was done

Spawned six parallel `general-purpose` verification subagents, one per page: `web-audio-fundamentals.md`, `scheduling-and-timing.md`, `synthesis-recipes.md`, `effects-and-mixing.md`, `audio-worklets-and-performance.md`, `javascript-music-libraries.md`. Per the queue's note that this domain is code/API-heavy rather than literature-heavy, each subagent was briefed to fetch the page's cited MDN/W3C/spec/GitHub/npm-registry sources directly and check specific numeric or API claims, rather than searching for corroborating papers. The main session also independently re-verified two numeric claims with `node -e` (float32 timestamp precision at 1/2/4/10-hour marks; the Karplus-Strong sampleRate/128 cutoff) before trusting a subagent's arithmetic.

Findings and fixes applied (all six pages promoted `draft` → `reviewed`):

- **synthesis-recipes.md** — real error: the xylophone mode-ratio table said "≈ 1, 3, ~9"; the ~9 partial belongs to marimba (already listed correctly at ≈1,4,10), not xylophone, whose third partial is ≈6 (commonly shorthanded "1:3:6" in the percussion-acoustics literature). Fixed.
- **scheduling-and-timing.md** — the claim that float32 timestamp resolution "at hour-scale timestamps is ~0.5 ms" overstated precision loss at the 1-hour mark by ~2x. Computed the real float32 ULP via bit manipulation in node: 0.244 ms at 1 h, 0.488 ms at 2 h, 0.977 ms at 4 h, 3.91 ms at 10 h. Reworded with the real numbers.
- **effects-and-mixing.md** — the Freeverb paragraph stated feedback≈0.84/damp≈0.2 and cited both the Stanford CCRMA page and the STK C++ port; the 0.84/0.2 figures are the original Jezar reference defaults, but the cited STK port actually ships its own altered defaults (≈0.91/0.1). Reworded to state both explicitly rather than implicitly misattributing STK's numbers. Also softened the BS.1770 K-weighting shelf-corner figure ("~1.5 kHz" → "~1.5–2 kHz", since sources place it closer to 2 kHz) and reworded "exponentially decaying noise" to "decaying noise" in the synthesized-IR code comment/prose, since the actual envelope (`Math.pow(1-x, decayPow)`) is a power-law decay, not a true exponential.
- **audio-worklets-and-performance.md** — the page asserted a specific Chromium/Firefox-dependable-vs-Safari-inconsistent compatibility matrix for Blob-URL `AudioWorklet.addModule()` under `file://`, citing WebAudio/web-audio-api-v2#109; the subagent fetched that issue and found it does not actually establish this matrix (it's about an `addModule(Blob)` proposal and a CSP caveat, not a browser-support table). Reworded to flag the cross-browser behavior as unsettled by any citable source and widened the existing open question from "test Safari" to "test all three browsers." Also added two smaller precision notes: Chrome's Media Engagement Index auto-allow is desktop-only, and the spec treats a worklet's `process()` returning `false` as only a hint (Chrome implements it as a hard, immediate teardown) — both surfaced by the subagent's MDN reads.
- **web-audio-fundamentals.md**, **javascript-music-libraries.md** — no factual errors found. The libraries page's version/license/publish-date/size claims for all ~11 surveyed libraries were checked directly against live npm registry JSON and GitHub license files and matched exactly (the page's self-reported "checked 2026-07-06" claim held up). The fundamentals page's `setTargetAtTime` percentages, a/k-rate assignments, `cancelAndHoldAtTime`'s continued absence from Firefox (re-confirmed live against Bugzilla #1308431, still NEW), and `renderSizeHint`'s absence from the MDN constructor page all held up on direct re-fetch.

No new page created this session; no cross-domain lint issues found (`node experiments/tools/wiki-lint.mjs` clean before and after, 61 pages throughout).

## Decisions / assumptions

- Treated the implementation domain's verification standard as "does the code/API claim hold up against a primary source (MDN, W3C spec, npm registry, GitHub repo)," per the queue's guidance to lean on API/spec checks rather than paper-fetching for this domain — worked well; every subagent found at least one small thing by actually fetching the cited source rather than trusting the existing citation.
- Left a few very-low-priority subagent suggestions unapplied as not worth the churn: Strudel's SPDX license id is technically `AGPL-3.0-or-later` (page says `AGPL-3.0`, common shorthand, not wrong); the DynamicsCompressorNode "~6 ms" lookahead figure was flagged uncertain by one subagent but then independently confirmed via the live W3C spec text by a different subagent (spec section 1.19.4, example code uses a 0.006 s compensating delay) — no action needed, already correct.

## Recommendations for next session

1. **Cross-domain lint** is now genuinely ripe (queue item 2): four domains matured (theory + psychology + algorithms + implementation = 23 `reviewed` pages of 61). A contradiction/duplication/stale-claim sweep across the `reviewed` set is a good next session, or could be combined with starting the next verify-and-mature domain (genres, per the stated order).
2. Remaining verify-and-mature order per `logs/status.md`: genres → craft → evaluation, plus the still-queued light verification pass on `indian-popular-and-film-music.md`.
3. Source notes in `wiki/sources/` (still empty) and Research questions R1–R8 remain untouched and high-value whenever a session wants a change of pace from the verification-pass rhythm.

## Session budget / tools note

No OpenRouter spend this session — all verification was done via WebFetch (MDN/W3C/GitHub/npm) and local `node` arithmetic checks, no need for a second-opinion model.
