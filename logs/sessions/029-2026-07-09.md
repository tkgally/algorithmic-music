# Session 029 — 2026-07-09

**Model:** Claude (Opus 4.8).
**Scope:** Tom answered the four comprehensive-site working-assumption questions from session 028. This session propagated those answers across the wiki and process docs, and carried out the structural consequences: reorganizing `docs/` so the five engines become *preliminary test engines* under `docs/preliminary-tests/` and `docs/index.html` becomes the comprehensive site's public entry point. Also re-checked `CLAUDE.md` and `README.md` for currency, as Tom requested.

## Tom's answers (primary source, preserved verbatim)

These arrived as the session prompt, appended after "Read continue-prompt.md and orient yourself to this project. The following are my answers to your questions in wiki/project-open-questions.md. Please update that and other pages based on my responses. Also check the current state of CLAUDE.md and README.md and make sure that they are up-to-date on the current state of this project." Quoted so future sessions (which cannot re-read the prompt) have the source:

> **Do Engines 01–05 stay live alongside the comprehensive site?**
> Tom: Yes, but put Engines 01–05 in a new `docs/preliminary-tests/` subfolder.
>
> **Where should the site live?** (GitHub Pages serves from `docs/`.)
> Tom: Put the index.html for this new site directly under `docs/`. It will be the entry point for this new site and when it has matured enough I will share that link with other people.
>
> **Roughly how many Start-level genre categories, and do you want to pick the shortlist?** (Working assumption: ~6–10; Claude proposes the shortlist in the control-taxonomy design doc for review.)
> Tom: That number and procedure sound good to me.
>
> **Review cadence for the design sessions.** (Working assumption: design docs reviewed asynchronously over multiple sessions; build the architecture only after those docs collect comments, unless "just build.")
> Tom: Yes, that sounds good. Go with your working assumption. From now [on], the documents and pages for my review should be put into `docs/` so that they are easy for me to review there. Any temporary documents and pages can be deleted from that folder after they are no longer needed.

## What was done

### 1. Propagated the four answers into the wiki

- **`project-open-questions.md`** — emptied "Questions for Tom" (none pending) and added a new "Comprehensive-site questions (answered 2026-07-09)" block (items 9–12) recording each decision and its propagation targets.
- **`comprehensive-site-vision.md`** — converted the three working-assumption passages to confirmed decisions: engines stay live and relocate to `docs/preliminary-tests/`; a new "Location (decided by Tom, 2026-07-09)" architecture bullet fixing the entry point at `docs/index.html` and the review-docs-in-`docs/` rule; the "Open questions" section retitled "Resolved and open questions."
- **`project-roadmap.md`** — Phase 2 paragraph rewritten (five engines shipped; relocation to `docs/preliminary-tests/`; reframed as preliminary test engines; entry point `docs/index.html`); Phase 3b now says review docs are prepared in `docs/`; the "engines remain live" line marked confirmed.

### 2. Reorganized `docs/` (the structural consequence of answers 1, 2, 4)

- **Relocated the hub + engines intact** with two `git mv`s: `docs/engines/` → `docs/preliminary-tests/engines/`, and `docs/index.html` (the old hub) → `docs/preliminary-tests/index.html`. Because each engine's back-link is the relative `../../index.html` (depth-2) and the hub's engine links are the relative `engines/…`, moving the whole unit down one level **preserved every internal link with zero edits to the engine folders** — so the five engines stay byte-frozen (important for the regression story). Git tracked all files as renames, preserving history.
- **New `docs/index.html` landing page** — the comprehensive site's public entry point. States the vision (compose/perform instrumental music across many familiar and on-the-fly-original styles), previews the three control tiers (Start/Intermediate/Advanced), is upfront that everything is written and built by Claude and dedicated to CC0, and links prominently to the preliminary tests so the working engines stay one click from the root. Marked "In early development" so it is honest about maturity (Tom will share the link "when it has matured enough"). Matches the existing hub's visual system.
- **Light reframing of the moved hub** (`docs/preliminary-tests/index.html`): title → "Preliminary test engines," an up-link to the main site (`../index.html`), and a lede that frames the five as the proving ground for the site being built. No engine cards changed.

### 3. Updated everything that pointed at the old paths

- **Harnesses (6):** the five `render-*.mjs` tools (`docs/engines/…` → `docs/preliminary-tests/engines/…`) and `ui-smoke.mjs` (now checks the new landing page links to the preliminary tests, checks the `preliminary-tests/index.html` hub lists all five engines, and drives each engine at its new `preliminary-tests/engines/…` path).
- **Wiki current-state pointers:** the five `findings-*-engine.md` pages and `music-representation-and-notation.md` (`docs/engines/…` → `docs/preliminary-tests/engines/…`; the tonal findings page's hub reference → `docs/preliminary-tests/index.html`). `updated:` bumped to 2026-07-09 on the four findings pages that were dated 2026-07-08.
- **Process docs:** `CLAUDE.md` (repo map row, hard rule 1, a new "Review docs → `docs/`" conventions bullet), `continue-prompt.md` (phase reminder), `README.md` (repo table, Status, Listening), `experiments/README.md` (vendoring paths).
- **`logs/status.md`:** new "As of" header (session 029, new structure), a reorganization bullet at the top of "Where things stand," the P3-2 queue item now noting review docs go in `docs/` and asking for the Start-genre shortlist, a new standing reminder, and a session-029 table row.

**Left as historical (intentionally):** `docs/engines/…` references inside dated session logs 018–028 and the existing dated entries in `wiki/log.md`. These narrate where things were at the time; rewriting them would falsify the record. Current-state documents all point at the new paths.

## Decisions & working assumptions (easy for Tom to override)

- **Kept an `engines/` grouping under `preliminary-tests/`** (`docs/preliminary-tests/engines/01-…`) rather than flattening to `docs/preliminary-tests/01-…`. Reason: relocating the hub+engines unit intact preserves every relative link and keeps the engines byte-frozen — the lowest-churn, lowest-risk reading of "put Engines 01–05 in a new `docs/preliminary-tests/` subfolder." If Tom prefers the flatter layout, it is a quick follow-up.
- **Made `docs/index.html` a real-but-early landing page now**, not a bare placeholder. Reason: GitHub Pages serves `docs/index.html` at the site root, so it must exist and not be broken; a short, honest landing page keeps the engines reachable and becomes the seed of the real site. It is clearly marked "In early development."
- **Wiki vs. `docs/` for design docs:** Tom's "review docs go in `docs/`" is now a standing rule. Working reconciliation with CLAUDE.md rule 4 (the wiki is the durable memory): documents *prepared for review* go in `docs/` and may be deleted once superseded, but any *durable decision* in such a doc is also filed in the wiki so it survives cleanup. Flagged in my summary to Tom in case he wants a different split.

## Validation

- `node experiments/tests/run.js` → **141 passed, 0 failed** (unchanged; the pure-Node suite doesn't touch `docs/` paths).
- `node experiments/tools/wiki-lint.mjs` → **0 problems across 98 pages**.
- `node experiments/tools/ui-smoke.mjs` → **19/19** — the new landing page links to the preliminary tests, the hub lists all five engines, and all five engines play live from `docs/preliminary-tests/engines/…` with zero console/page errors.
- `node experiments/tools/render-engine.mjs` → **16/16**; `node experiments/tools/render-percussion.mjs` → **43/43** — offline render paths (`_selftest.html`) resolve from the new location. (The other three render tools were changed by the same mechanical substring rename and share the harness pattern; these two confirm both ends of the numbered range.)

## Recommended next steps

- **P3-1** still stands: verification passes over the 26 session-028 draft pages (design pillar first). Unchanged by this session.
- **P3-2 is now cleanly unblocked and the natural next scope:** the first design document — the **control taxonomy** — proposed in `docs/` for Tom's review (he explicitly wants to review it). It should include the concrete ~6–10 Start-genre shortlist with the wiki page behind each, and the Start/Intermediate/Advanced control lists (names, ranges, defaults, mappings), building on `control-surfaces-and-user-parameters.md`. This is the first exercise of the new "review docs go in `docs/`" rule — decide the `docs/` filename/location convention for review docs when writing it (e.g., `docs/design/…`).
- Minor carry-over: **P3-3** (reconcile `engine-architecture.md`'s documented `freqHz` to the shipped `freq`, and document the `beat`/`midi`/`tags`/`weight?`/`expr?` pass-through) — a small doc fix surfaced in session 028.
